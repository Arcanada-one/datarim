---
id: PRD-TUNE-0585
title: Universal customer-delivery canon
status: approved
version: 1.0
created: 2026-08-22
author: Codex under operator-approved autonomous execution
---

# PRD-TUNE-0585 — Universal Customer-Delivery Canon

## Context

Datarim already preserves the operator's verbatim brief in `init-task.md`, translates it into human wishes in `expectations.md`, and verifies implementation through PRD, plan, QA, compliance, and archive stages. The current contracts do not prove that a customer remark became a delivered customer outcome. In particular, `pending` expectations can pass verification, production proof is armed only for selected deploy-class tasks, and an implementation artifact can be described as complete without a complete atomic chain back to the customer remark.

Talomnia exposed the consequence at epic scale: review findings produced tools, documents, and checks while customer-visible design and White Paper requirements could remain open or be rediscovered. The framework needs a universal contract before Talomnia is reconciled again.

## Problem

The pipeline lacks a machine-verifiable chain from customer language to live delivery. It therefore cannot answer, without narrative judgment:

1. Was every verbatim customer remark preserved?
2. Was each remark decomposed into atomic requirements?
3. Did each requirement have an honest visitor-visible production AC?
4. Were all required capabilities pinned before work began?
5. Did customer review cause knowledge evolution before remediation?
6. Can the tests detect absence or regression?
7. Which exact revision is deployed, and what does a visitor see?
8. Did the customer accept, return, defer, or reject the result?
9. Is the epic complete by coverage rather than by a stored status word?

## Goals

- Add one universal, versioned customer-delivery contract usable by every Datarim-managed project.
- Preserve the init-task and expectations contracts as distinct human-intent layers.
- Enforce pre-work capability binding and review-to-evolution before implementation.
- Enforce live evidence for visitor-visible work and reject artifact-only self-certification.
- Derive task and epic delivery state from source and evidence coverage.
- Provide deterministic validation, actionable missing-ID output, templates, migration guidance, and lifecycle wiring.
- Dogfood the contract in TUNE-0585 before registering Talomnia as the first external consumer.

## Non-Goals

- Implement a universal knowledge graph or require one storage backend.
- Infer whether a customer likes a result; disposition must come from a customer-authority record.
- Merge operator-signature or legal-approval artifacts automatically.
- Perform the downstream Talomnia content/design work inside the framework repository.
- Replace existing PRD V-AC, expectations, mutation, CI, deploy, or archive gates.

## Solution Approaches

### Approach A — Append-only JSONL delivery ledger plus deterministic writer and validator (selected)

Add `datarim/delivery/{TASK-ID}.jsonl` as the machine source of truth. A standard-library Python writer atomically appends hash-chained events; a read-only validator checks schema, source coverage, temporal ordering, evidence completeness, screenshot coverage, customer disposition, and derived epic state. Commands consume the same validator at phase-specific gates.

Advantages: clean separation from human wishes; safe verbatim strings; correction without history rewriting; deterministic cross-task coverage; direct adoption by non-Markdown projects; exact missing requirement IDs. Trade-off: one additional per-task artifact plus a writer and validator.

### Approach B — Expectations schema v4

Add all delivery fields to each expectation item. This reduces file count but conflates human wishes with atomic requirements, cannot naturally represent one remark mapping to several requirements, and makes the existing Markdown parser materially more fragile.

### Approach C — Archive checklist only

Require a prose delivery table at archive. This is cheap but post-hoc by construction, cannot block implementation without pinned capabilities, and repeats the failure class the task exists to remove.

## Selected Architecture

### 1. Artifact

`datarim/delivery/{TASK-ID}.jsonl` is a versioned append-only event stream. Each line carries `schema_version`, `event_id`, `event_type`, `task_id`, `recorded_at`, `payload`, `prev_event_hash`, and `event_hash`. Writers take a per-task lock, recompute the predecessor hash, write to a temporary file, validate the full stream, and atomically replace the ledger. Corrections append a superseding event; no event is edited or deleted.

Normative event types are `lane_classified`, `source_manifest_bound`, `remark_captured`, `requirement_declared`, `production_ac_declared`, `atomicity_reviewed`, `parent_intent_bound`, `evolution_requested`, `evolution_blocked`, `evolution_approved`, `evolution_rejected`, `evolution_declined`, `evolution_applied`, `evolution_reviewed`, `evolution_disposition`, `prework_contract_frozen`, `implementation_authorized`, `implementation_bound`, `red_observed`, `green_observed`, `production_deployment_observed`, `visual_observation`, `customer_disposition`, `enabling_disposition`, `child_manifest_bound`, `legacy_claim_imported`, `head_reconciled`, and `event_superseded`.

Derived delivery state is never authored as an authoritative event. The validator calculates it from the current non-superseded event graph every run. Anchor receipts are external provider records, never in-ledger events, so anchoring cannot recurse. A locally valid chain is insufficient: every normal strict stage transition requires an authenticated receipt for the exact final event hash, event count, task ID, project ID, immutable Git blob OID, and content length. The provider acknowledges only after the complete blob is retrievable from its immutable/content-addressed store or protected Git ref; resolve re-downloads the bytes and recomputes both blob and event hashes. A shorter, divergent, missing, or unretrievable local/provider chain fails even when internal hashes are valid.

Canonical event bytes are precisely defined. JSON input rejects duplicate keys and accepts only strings, booleans, null, arrays/objects, and signed 64-bit integers; floats are forbidden. Strings are valid UTF-8 and retain their exact Unicode scalar sequence (no normalization). The hash preimage is the event object without `event_hash`, serialized as UTF-8 with keys sorted by Unicode code point, no insignificant whitespace, JSON lowercase literals, and minimal base-10 integers. String escaping is exact: quotation mark becomes `\"`; reverse solidus becomes `\\`; U+0008, U+000C, U+000A, U+000D, and U+0009 become `\b`, `\f`, `\n`, `\r`, and `\t`; every other U+0000-U+001F scalar becomes `\u00xx` with lowercase hexadecimal digits. U+2028 and U+2029 remain unescaped UTF-8. No solidus escaping, ASCII-only transformation, BOM, or trailing newline is permitted. `prev_event_hash` is inside the preimage. Shipped cross-runtime test vectors fix every boundary.

Schema v1 fixes the full event envelope. Every line has exactly `schema_version` (integer `1`), `event_index`, `event_id`, `event_type`, `project_id`, `task_id`, `recorded_at`, `payload`, `prev_event_hash`, and `event_hash`; unknown or missing keys fail. `event_index` is the zero-based signed 64-bit non-negative physical line index. IDs are non-empty ASCII strings matching `^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$`. `recorded_at` is UTC `YYYY-MM-DDTHH:MM:SSZ` with no fraction. The genesis predecessor is exactly 64 lowercase zeroes. Every later predecessor equals the prior line's `event_hash`. `event_hash` is the 64-character lowercase hexadecimal SHA-256 of the canonical preimage. The stored line is the canonical full event including `event_hash` followed by exactly one LF; LF is not hashed, blank lines and a missing final LF fail. Arrays retain declared order; set-like arrays identified below must already be ascending by canonical byte value with no duplicates.

Schema v1 fixes primitive and reusable payload types. `id128` is an ASCII string matching `^[A-Za-z0-9][A-Za-z0-9._:/-]{0,127}$`; `sha256` is exactly 64 lowercase hexadecimal characters; `git_oid` is exactly 40 lowercase hexadecimal characters; `utc_seconds` matches `^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$` and must be a real UTC date; `text` is 1–4096 Unicode scalars with no U+0000; `relative_path` is 1–1024 UTF-8 bytes, uses `/`, is not absolute, and has no empty, `.`, `..`, or symlink-resolved escaping segment; `locator` is either a `relative_path` or a 1–2048-byte registered URI with no control scalar; `https_url` is an absolute HTTPS URL with no credentials or fragment. Integers are signed 64-bit; counts, indexes, dimensions, byte lengths, registry/provider sequences, and resume cursors are non-negative. No payload value is null unless explicitly allowed below.

`authority` has exactly `registry_revision:uint`, `role:authority_role`, `key_id:id128`, and `namespace:id128`. It does not contain the event signature: the configured receipt provider resolves a detached `event-attestation` by `(project_id, task_id, event_id, event_hash)`, preventing signature self-reference, and the validator verifies it before accepting the event. That attestation is signed by the exact active key named by `authority.key_id`; its signer role and namespace must equal the event-to-authority mapping below at the named registry revision. The append provider separately authenticates publication/control records as `anchor-authority` and can never substitute that anchor signature for event authorship. `artifact_ref` has exactly `id:id128`, `revision:text`, `sha256:sha256`, `locator:locator`, and `binding_commit:git_oid`. `receipt_ref` has exactly `profile:id128`, `locator:locator`, `sha256:sha256`, `signer_key_id:id128`, and `namespace:id128`; it refers to an independently existing provider record, never a signature over the event containing the reference. `conflict_ref` has exactly `event_id:id128` and `head_hash:sha256`. `typed_predicate` has exactly `kind`, `selector`, `operator`, and `expected`; `kind` is one of `dom`, `http`, `image`, `cli`, `api`, or `document`; `selector` is `text`; `operator` is one of `equals`, `contains`, `matches`, `exists`, `not_exists`, `http_status`, `image_digest`, or `dom_fingerprint`; `expected` is a string, boolean, null, signed 64-bit integer, or an ordered array of those scalars. Every payload contains non-null `authority` plus exactly the following type-specific keys; every `[]` value is a duplicate-free ascending array by canonical bytes unless explicitly described as ordered:

For event index `n`, the `event-attestation` subject's `ledger_preimage_sha256` is SHA-256 of the exact stored ledger-prefix bytes for physical lines `[0,n)`, including every prior line's terminating LF; for genesis it is SHA-256 of the empty byte string. It is distinct from `event_hash`, which hashes the current canonical event preimage.

| Event type | Exact type-specific payload keys |
|---|---|
| `lane_classified` | `lane`, `reason`, `source_receipt` |
| `source_manifest_bound` | `provider_id`, `manifest_id`, `manifest_sha256`, `manifest_anchor`, `item_count`, `exhausted` |
| `remark_captured` | `remark_id`, `manifest_item_id`, `pseudonymous_item_id`, `byte_length`, `commitment_profile`, `key_id`, `commitment`, `source_kind`, `authority_class`, `opaque_locator` |
| `requirement_declared` | `requirement_id`, `remark_ids[]`, `requirement_class`, `title` |
| `production_ac_declared` | `requirement_id`, `ac_id`, `blueprint_ref`, `surface_id`, `predicate` |
| `atomicity_reviewed` | `source_manifest_sha256`, `requirement_graph_sha256`, `mapping_cardinality`, `reviewer_receipt`, `predecessor_head`, `spec_revision`, `verdict` |
| `parent_intent_bound` | `requirement_ids[]`, `parent_project_id`, `parent_task_id`, `parent_requirement_id`, `source_receipt` |
| `evolution_requested` | `request_id`, `replaces_request_id`, `requirement_ids[]`, `artifact_class`, `artifact_before`, `proposed_action`, `owner_role`, `resume_cursor` |
| `evolution_blocked` | `request_id`, `requirement_ids[]`, `reason_code`, `approval_required`, `resume_cursor` |
| `evolution_approved` | `request_id`, `requirement_ids[]`, `approval_receipt`, `resume_cursor` |
| `evolution_rejected` | `request_id`, `requirement_ids[]`, `rejection_receipt`, `reason`, `resume_cursor` |
| `evolution_declined` | `request_id`, `requirement_ids[]`, `decline_receipt`, `terminal`, `resume_cursor` |
| `evolution_applied` | `request_id`, `requirement_ids[]`, `artifact_before`, `artifact_after`, `resume_cursor` |
| `evolution_reviewed` | `request_id`, `requirement_ids[]`, `artifact_after`, `reviewer_receipt`, `verdict`, `resume_cursor` |
| `evolution_disposition` | `request_id`, `requirement_ids[]`, `disposition`, `covering_artifacts[]`, `reason`, `resume_cursor` |
| `prework_contract_frozen` | `contract_base_commit`, `contract_manifest`, `requirement_graph_sha256` |
| `implementation_authorized` | `repository_id`, `clean_base_sha`, `freeze_commit`, `marker_commit`, `marker_path`, `marker_sha256`, `dirty_inventory_sha256`, `authorization_receipt` |
| `implementation_bound` | `repository_id`, `authorization_event_id`, `first_implementation_commit`, `implementation_tree_sha256` |
| `red_observed` | `requirement_id`, `assertion_id`, `argv[]` (ordered), `exit_code`, `repository_sha`, `hermeticity_sha256`, `output_sha256`, `evidence_path`, `mutation_id`, `mutation_diff_sha256`, `failed_assertion`, `runner_receipt` |
| `green_observed` | `requirement_id`, `assertion_id`, `argv[]` (ordered), `exit_code`, `repository_sha`, `hermeticity_sha256`, `output_sha256`, `evidence_path`, `restored_tree_sha256`, `runner_receipt` |
| `production_deployment_observed` | `requirement_id`, `source_sha`, `deployed_sha`, `build_provenance`, `deployment_receipt` |
| `visual_observation` | `requirement_id`, `locale`, `viewport`, `theme`, `screenshot_path`, `screenshot_sha256`, `width`, `height`, `url`, `deployed_sha`, `dom_fingerprint`, `assertion_result`, `observed_at`, `provider_receipt` |
| `customer_disposition` | `requirement_id`, `deployed_sha`, `disposition`, `customer_record` |
| `enabling_disposition` | `requirement_id`, `disposition`, `project_receipt` |
| `child_manifest_bound` | `provider_id`, `manifest_id`, `manifest_sha256`, `manifest_anchor`, `child_count`, `exhausted` |
| `legacy_claim_imported` | `claim_id`, `claim_kind`, `source_locator`, `source_sha256`, `reason_non_authoritative` |
| `head_reconciled` | `conflicts[]`, `chosen_successor_event_id`, `reconciliation_receipt` |
| `event_superseded` | `target_event_id`, `target_event_type`, `replacement_event_id`, `reason`, `signer_scope`, `invalidation_set[]` |

The shipped `config/customer-delivery-event-v1.schema.json` is an exact executable transcription; it cannot invent additional restrictions. Field types are closed as follows:

- all names ending `_id` or `_profile` and the fields `lane`, `profile`, `key_id`, `source_kind`, `authority_class`, `requirement_class`, `artifact_class`, `proposed_action`, `owner_role`, `reason_code`, `verdict`, `disposition`, `claim_kind`, `locale`, `viewport`, `theme`, `signer_scope`, and `repository_id` are `id128` enums or IDs as enumerated below; `replaces_request_id` is `id128|null`;
- `event_type` and `target_event_type` are members of the normative event enum;
- every field ending `_sha256`, plus `commitment`, `predecessor_head`, and `head_hash`, is `sha256`;
- `contract_base_commit`, `clean_base_sha`, `freeze_commit`, `marker_commit`, `first_implementation_commit`, `repository_sha`, `source_sha`, `deployed_sha`, and `spec_revision` are `git_oid`;
- fields ending `_path` are `relative_path`; `opaque_locator`, `source_locator`, and every nested `locator` are `locator`; `url` is `https_url`; `recorded_at` and `observed_at` are `utc_seconds`;
- `reason`, `title`, `failed_assertion`, and `reason_non_authoritative` are `text`; `item_count`, `child_count`, `byte_length`, `width`, `height`, `exit_code`, and `resume_cursor` are bounded integers; `exhausted`, `approval_required`, `terminal`, and `assertion_result` are booleans, and `terminal` must be true;
- `mapping_cardinality` has exactly `remark_count:uint`, `requirement_count:uint`, and `edge_count:uint`; `predicate` is a `typed_predicate`; `artifact_before`, `artifact_after`, and `blueprint_ref` are `artifact_ref`, with only `artifact_before` nullable and only for `proposed_action=create`;
- all fields ending `_receipt`, `_record`, or `_anchor`, plus `source_receipt`, `build_provenance`, and `customer_record`, are `receipt_ref`;
- `remark_ids`, `requirement_ids`, and `invalidation_set` contain `id128`; `contract_manifest` is `artifact_ref`; `covering_artifacts` contains `artifact_ref`; `argv` is an ordered array of 0–65535-byte UTF-8 strings without U+0000; `conflicts` contains at least two `conflict_ref` records.

Enumerations are exact. `authority_role` is one of `governance`, `registry-owner`, `spec-reviewer`, `atomicity-reviewer`, `evolution-owner`, `evolution-reviewer`, `quality-reviewer`, `evidence-runner`, `project-verifier`, `customer-authority`, `source-provider`, `deployment-provider`, `visual-provider`, `release-authority`, `anchor-authority`, or `reconciliation-authority`. Lanes are `customer-visible-v1`, `enabling-v1`, or `none`; requirement classes are the first two. Source kinds are `customer_review`, `customer_brief`, `operator_amendment`, `customer_document`, or `legacy_unverified_source`; authority classes are `customer`, `operator`, `project`, or `legacy-unverified`.

Artifact classes are `role`, `skill`, `blueprint`, `constraint`, `policy`, or `success-criterion`; owner roles are `architect` or `skill-creator`; proposed actions are `create`, `revise`, `reuse`, or `no-change`; reason codes are `approval_required`, `changes_required`, `provider_unavailable`, or `authority_rejected`; review verdicts are `pass` or `fail`; evolution dispositions are `reused`, `revised`, `created`, or `no-change`; customer dispositions are `accepted`, `returned`, `deferred`, `rejected`, or `withdrawn`; enabling disposition is only `verified`; legacy claim kinds are `implementation`, `test`, `screenshot`, or `deployment`; locales are `ru` or `en`, viewports `mobile` or `desktop`, and themes `light` or `dark` for `bilingual-web-v1` (other versioned blueprints require a new enum-bearing schema version). `head_reconciled.conflicts` must pair every divergent event with its head, the pairs must share one predecessor, and `chosen_successor_event_id` must equal exactly one paired event ID. Any future payload field, enum value, or event type requires a new schema version and migration, never permissive schema-v1 parsing.

### 2. Atomic identity and source coverage

`remark_id` and `requirement_id` are immutable within a task. Every remark maps to at least one requirement. Every requirement maps back to at least one remark. Duplicate verbatim remarks may share a content digest but retain distinct source locators. A one-to-many decomposition is explicit. For an armed lane, a missing genuine source blocks: agent-authored Overview prose can be retained only as `legacy_unverified_source`, never as `verbatim` customer provenance.

The denominator is not the ledger's self-declared `remark_captured` set. `source_manifest_bound` pins an authoritative, provider-produced inventory of complete source containers and stable item identifiers: the init-task brief and append-log records for native tasks, plus the project registration's issue/comment/document adapters for imported work. The validator independently resolves every manifest item, recomputes its bytes and digest, and rejects uncaptured, multiply captured, missing, or extra source items. A source manifest is valid only when the adapter proves pagination/exhaustion and its exact manifest head is retained by the monotonic anchor authority. Semantic atomization is then independently reviewed: a single source item may produce several requirements, but it cannot disappear through aggregation.

Verbatim bytes are private data, not necessarily public Git data. Native tasks keep them in the gitignored init-task/private source store; imports may use a registered encrypted or access-controlled source vault. The public ledger records a pseudonymous item ID, byte length, versioned HMAC-SHA-256 commitment, source-kind, authority class, and an opaque locator—not the private text—unless the project explicitly classifies that text as publishable. The HMAC key remains in the private provider. For commitment profile `datarim-source-hmac-sha256-v1`, the HMAC preimage is the canonical-JSON byte serialization defined above of exactly this object and no other framing bytes: `{"byte_length":N,"item_id":"...","key_id":"...","profile":"datarim-source-hmac-sha256-v1","project_id":"...","schema_version":1,"source_b64":"...","task_id":"..."}`. `source_b64` is RFC 4648 standard-alphabet Base64 of the original source bytes, with required `=` padding and no whitespace or line wrapping; `N` is the original byte length. HMAC-SHA-256 is computed over those exact UTF-8 canonical bytes, and the public commitment is lowercase hexadecimal. This domain separation binds profile/schema/project/task/item/key ID and length without delimiter ambiguity. Rotation cross-signs old/new key metadata, retains the old key for historical verification, and never rewrites commitments. The source provider must return the original bytes for validation without logging them. Deletion or alteration fails the anchored commitment; public identifiers cannot expose provider-native issue/comment/user IDs. Reports redact content by default.

### 3. Delivery lanes and surface blueprints

- `customer-visible-v1`: requirements must carry a production route/surface and the complete live proof chain.
- `enabling-v1`: requirements may close on their own implementation evidence only when they name a customer-visible parent requirement in an authoritative manifest. They never mark the parent delivered.
- `none`: the task contains no customer-delivery requirement. It records a reason and cannot be used for a task whose init-task or append-log contains a customer remark or visitor-facing outcome.

Every task classifies its lane, so the canon is universal. The expensive production matrix is hard only where semantics require it, which permits private research, legal, documentation, API, CLI, framework, and tooling work to close honestly without allowing it to satisfy customer-facing work.

Customer-visible and enabling dispositions are deliberately different. Customer-visible completion requires customer-authenticated `accepted` or denominator-removing `withdrawn`. An enabling requirement may close only with a project-authority-signed `verified` disposition after its implementation/RED/GREEN chain passes. It contributes zero customer-delivery coverage to its parent. `parent_intent_bound` may bind a pre-registration parent from an authenticated operator/customer source record; later project registration must resolve to the same project/task/requirement identity or the enabling task reopens.

Each customer-visible requirement names a pinned surface blueprint with typed executable predicates. `bilingual-web-v1` requires the exact RU/EN × mobile/desktop × light/dark eight-cell matrix and may be widened but not narrowed by registration. Other shipped blueprints define equivalent visitor-observation dimensions for native, CLI, API, and published-document surfaces; none permits tools, source documents, tests, or local-only output to substitute for the actual customer-consumed production surface. Talomnia uses `bilingual-web-v1`.

Atomicity is structural and independently reviewed: one requirement has one subject, one observable behavior, one terminal disposition, and one surface blueprint; its production AC is a typed predicate plus expected value, not prose alone. A source item that contains conjunctions, multiple routes, or independently rejectable outcomes must decompose. `atomicity_reviewed` binds the authoritative source-manifest digest, exact ordered digest of all active requirement and production-AC events, complete remark-to-requirement cardinality, reviewer identity/trust proof, exact predecessor ledger head/spec revision, and verdict. Any later declaration, amendment, or supersession of a source/requirement/AC invalidates the review and downstream freeze. A passing review receipt is required before freeze; aggregate catch-all requirements fail review and validation.

### 4. Pre-work binding

Before implementation starts, every armed requirement must pin exactly one or more immutable references for each class: role, skill, blueprint, constraint, policy, and success criterion. Each reference carries a logical ID, immutable revision, content digest, binding record locator, and binding commit. The root-confined contract manifest has exactly `schema_version:uint=1`, `project_id:id128`, `task_id:id128`, `requirement_graph_sha256:sha256`, and `requirement_bindings[]`. Each binding has exactly `requirement_id:id128`, `parent_intent_event_ids[]:id128`, `evolution_disposition_event_ids[]:id128`, `role_refs[]:artifact_ref`, `skill_refs[]:artifact_ref`, `blueprint_refs[]:artifact_ref`, `constraint_refs[]:artifact_ref`, `policy_refs[]:artifact_ref`, and `success_criterion_refs[]:artifact_ref`. Bindings are sorted by requirement ID, contain every active requirement exactly once, contain no inactive/foreign requirement, and every event edge resolves to an earlier active event whose own `requirement_ids` contains that requirement. Each capability array is non-empty. To avoid a Git fixed point, `prework_contract_frozen` names the already-existing `contract_base_commit` plus `contract_manifest:artifact_ref`; the referenced manifest already exists at its locator/commit/digest and the event never claims the commit containing itself. The validator derives `freeze_commit` as the first descendant whose tree contains the exact ledger blob/freeze event and verifies that commit independently. `implementation_authorized` then names that known freeze commit, the exact clean implementation-repository base SHA, a previously committed authorization-marker commit/path/digest, and an external authorization receipt. It never names the commit containing itself. The validator derives the authorization-event commit from Git, and implementation begins only after that commit exists. In a same-repository task, the first implementation commit descends from the authorization-event commit; across repositories, it descends from the named marker commit and the external receipt's independently signed append-result provider sequence proves authorization-event publication preceded work. The validator rejects:

- literal or semantic `Unbound`;
- mutable revisions such as `main`, `master`, `latest`, branch-only URLs, or unversioned paths;
- any missing capability class;
- a contract base/freeze commit that does not predate the implementation authorization;
- a freeze event absent from the uniquely derived freeze commit or mismatching its contract manifest;
- a missing authorization marker, dirty-worktree inventory, mismatched target-repository identity/base SHA, or implementation commit that is not a descendant of the marker commit;
- an evolution disposition written after `implementation_bound`.

Timestamps are descriptive, not ordering authority. Event order, hash-chain integrity, retained ledger blobs, the committed authorization marker, and implementation-repository Git ancestry establish the auditable ordering contract. The framework cannot prove the absence of deliberately hidden off-ledger work; such bypass is a detectable policy violation and cannot be imported later as canon-compliant pre-work.

### 5. Review-to-evolution gate

Every requirement sourced from a customer review must record one pre-work disposition:

- `reused`: existing pinned artifacts already cover the requirement;
- `revised`: one or more existing artifacts were revised and pinned;
- `created`: missing artifacts were created and pinned;
- `no-change`: explicit finding that no evolution is needed, with pinned covering artifacts and a reason.

The pre-work gate blocks `/dr-do` when the disposition or immutable references are absent. Writing an evolution note after implementation does not clear the gate.

Review origin is derived from the authoritative source provider's immutable source-kind metadata (`customer_review`, `customer_brief`, `operator_amendment`, or another registered kind), not selected by the implementer. `customer_review` and amendments to prior delivered work always arm this gate.

`/dr-plan` owns and invokes a bounded pre-work evolution substage before authorization. Owner mapping is registry-derived, not implementer-selected: blueprint/policy/constraint/success-criterion → `architect`; skill/role → `skill-creator`; a project may narrow authority but never grant the implementer approval authority. Each request's `resume_cursor` starts at 0 and increments by exactly one on every event carrying that request ID; a duplicate cursor with identical event digest is an idempotent replay, while a gap, regression, or different event at the same cursor is integrity failure.

The transition table is closed. `reuse` and `no-change` emit only `evolution_disposition(cursor=0, reused|no-change)` with covering artifacts and may freeze. `create`/`revise` emit `evolution_requested(cursor=0)`. When approval is required, the only next event is `evolution_blocked(cursor=1, approval_required)`, followed by exactly one `evolution_approved(cursor=2)` or `evolution_rejected(cursor=2)`; approval then permits `evolution_applied(cursor=3)`, while rejection permits only `evolution_declined(cursor=3, terminal=true)` and blocks freeze. A rejected request cannot be replaced until that decline exists; the replacement names the declined request. When approval is not required, request transitions directly to `evolution_applied(cursor=1)` and an approval/rejection event is invalid. Instead of either approval path, an owner/provider failure immediately after a request may emit `evolution_blocked(cursor=1, provider_unavailable|authority_rejected, approval_required=false)` and remains blocked. Applied transitions only to `evolution_reviewed(next cursor)`. Review `pass` transitions only to `evolution_disposition(next cursor, created|revised)` and may freeze. Review `fail` transitions only to `evolution_blocked(next cursor, changes_required, approval_required=false)` and remains blocked. A declined or blocked changes-required/provider-unavailable/authority-rejected request can resume only with a new `evolution_requested(cursor=0)` whose `replaces_request_id` names that terminal/blocked request; reuse of the old request ID is forbidden. `evolution_requested` therefore additionally has required `replaces_request_id:id128|null`; it is null only for the first attempt.

Every transition verifies request ID, prior ledger head, event-specific artifact before/after digest, owner trust proof, approval/review record where required, and cursor. A crash resumes at the first missing valid transition, never against an unapproved/newer artifact. Task-scoped reversible artifacts are changed and reviewed only in the approved/no-approval branch, then control returns to `/dr-plan` for refreeze. Framework-wide operating-model changes follow the existing Class B approval boundary. The task cannot enter `/dr-do` until all active requests have a passing disposition and the pinned artifacts exist in the derived freeze commit.

Every transition additionally requires an identical non-empty canonical `requirement_ids` set across the entire request branch. A replacement request may narrow or widen only by presenting its own explicit set and is independently reviewed; no prior requirement inherits a disposition implicitly.

### 6. Evidence model

Every requirement needs load-bearing RED and GREEN events emitted by the framework evidence runner, not hand-authored metadata. They name a shared assertion ID, argv, exit code, observed repository SHA, hermeticity inputs, output digest, and retained evidence path. RED must be non-zero, GREEN zero, and both must name the same load-bearing assertion. The RED record also names the intentional mutation/negative control and the exact assertion that failed; setup, wrapper, or unrelated failures do not count. The runner produces a signed attestation over the command, repository tree, mutation diff, output, and result using a trust root pinned in the consumer registry (`git-ssh-v1`, `sigstore-oidc-v1`, or another shipped verifier profile); an unauthenticated digest alone never proves execution. An independent quality review checks exact-head provenance. Its signed receipt is deliberately external to the reviewed implementation head: it binds repository ID, exact commit/tree, ledger blob/head, reviewer role/key, finding digest, verdict, and timestamp, and is compare-and-appended to the protected receipt authority. Appending the receipt never mutates the reviewed head. A superseding review of a later head creates a new external record; no in-ledger or working-tree receipt may satisfy exact-head review. For migrated work, a present-day mutation or negative control may provide RED; historical RED is never invented. For customer-visible requirements the close gate additionally requires:

- exact merged and deployed SHA, with equality or an explicit build-provenance link;
- live HTTP and DOM evidence captured after deployment, including expected route identity and semantic assertions so an HTTP error or generic shell cannot pass;
- eight screenshot cells: RU and EN × mobile and desktop × light and dark;
- screenshot file digest, dimensions, viewport, theme, locale, URL, deployed SHA, DOM/content fingerprint, assertion result, and timestamp for every cell; cells cannot alias one retained file or one effective locale/theme/viewport state;
- customer disposition: `accepted`, `returned`, `deferred`, `rejected`, or `withdrawn`, resolved from an authenticated customer-authority record with stable record ID, signer/identity proof, verbatim private-source locator/digest, timestamp, and exact requirement/deployed-SHA binding. The event is only a reference; the validator refetches and verifies the registered authority record. Only customer-authenticated `withdrawn` removes a requirement from the coverage denominator; `deferred` remains incomplete.

Tests, tools, source documents, CI, and local screenshots are supporting evidence. None can substitute for the deployed/live/screenshot/disposition chain of a visitor-visible requirement.

### 7. Derived coverage

The validator reports deterministic counts and exact missing IDs for:

- remark-to-requirement coverage;
- visitor-AC coverage;
- pre-work capability coverage;
- review-to-evolution coverage;
- implementation and RED/GREEN coverage;
- deployment/live coverage;
- screenshot-cell coverage;
- customer-disposition coverage;
- child-contract coverage for epics.

Customer-visible task delivery is complete only when all in-scope requirements pass and every in-scope requirement has customer-authenticated `accepted`, or has been removed from the denominator by customer-authenticated `withdrawn`. `returned` and `rejected` derive `rework_required`; `deferred` derives `blocked`. Enabling task delivery is complete only when every enabling requirement passes implementation/RED/GREEN and carries project-authority `verified`; that state never changes the parent denominator. Epic delivery is complete only when its own customer-visible requirements and every child from the independently resolved child manifest pass. Child references must be acyclic and resolve to the same monotonic project registry; cycles, missing children, or cross-project identity mismatches fail closed. A ledger-authored child list or stored `completed` status cannot override the provider manifest or a failing derived report. Suggested derived states are `not_started`, `in_progress`, `customer_review`, `rework_required`, `blocked`, and `complete`.

### 8. Lifecycle wiring

- `/dr-init`: classify the delivery lane; for armed lanes create the ledger and capture every initial verbatim remark as a distinct event.
- `/dr-prd`: atomize remarks, add typed production ACs/surface blueprints, and bind requirements to V-AC.
- `/dr-plan` and `/dr-design`: pin all capability and evolution references before implementation.
- `/dr-do`: run pre-work validation before source edits; require a committed freeze event before the first implementation commit; append implementation and RED/GREEN evidence after work.
- `/dr-qa`: verify implementation evidence and, for deployed work, the live matrix without accepting artifact-only completion.
- `/dr-compliance`: hard-enforce contract and coverage completeness for the task's declared delivery stage.
- `/dr-archive`: require derived complete state; unresolved customer-visible requirements block archive. An operator-authorized deferral may stop work at `blocked`, but it does not convert the requirement or epic to `complete`.
- `/dr-auto`: choose the next reversible action from exact missing requirement IDs, never from narrated epic status.
- `/dr-status`: render only derived coverage/state for armed tasks and epics.
- `/dr-quick`: classify first; customer-visible work either executes the same hard gates or routes into the full pipeline before any implementation.
- `/dr-write`, `/dr-edit`, and `/dr-publish`: classify customer/content scope, preserve source remarks, and never treat draft preparation or publication tooling as visitor-visible delivery. Actual publication remains operator-gated.
- `/dr-next` and `/dr-orchestrate`: replay and dispatch from the derived next missing gate without bypassing lane enforcement.

The shared command router requires classification before any task-creating or task-advancing command. The named command integrations are regression-tested, but the hard invariant is capability-based: a new entry point that can create, implement, review, close, publish, or archive work fails validation until it declares its customer-delivery behavior.

### 9. Project registration

The framework ships a project-registration template. Consumers register project ID, epic root, authoritative source and child-manifest providers, contract roots, public surface base URLs, surface blueprints, knowledge-reference resolver, deployment provenance probe, customer-authority policy, and a framework-release `canon_epoch`. The universal validator accepts adapters but keeps the core schema project-agnostic.

Trust has two signed layers. The framework catalog `config/customer-delivery-trust-catalog.json` and detached `config/customer-delivery-trust-catalog.json.sig` fix schema/canonicalization versions, minimum verifier/profile versions, permitted algorithms/namespaces/receipt kinds, and recovery rules under the pre-existing Datarim governance root. The concrete Datarim consumer `config/customer-delivery-datarim-registry.json` and detached `config/customer-delivery-datarim-registry.json.sig` reference the exact catalog digest and instantiate repository/project adoption state, `canon_epoch`, providers, signers, scopes, revocation sequence, and monotonic registry predecessor. `templates/customer-delivery-project.json` carries the same consumer schema for downstream projects; the template alone is insufficient. `config/customer-delivery-allowed-signers` is the exact public-key view used by the SSH verifier.

Catalog schema v1 has exactly `schema_version:uint=1`, `catalog_id:id128`, `revision:uint`, `valid_from:utc_seconds`, `valid_until:utc_seconds|null`, `canonicalization_profiles[]`, `receipt_profiles[]`, `signature_profiles[]`, `minimum_profile_versions[]`, and `recovery_policy`. A canonicalization-profile record has exactly `profile:id128`, `version:uint`, `hash_algorithm:sha256-literal`, and `event_schema_sha256:sha256`; a receipt-profile record has exactly `receipt_type:id128`, `version:uint`, `namespace:id128`, `algorithm:ssh-ed25519-literal`, and `subject_schema_sha256:sha256`; a non-receipt signature-profile record has exactly `profile:id128`, `version:uint`, `namespace:id128`, `algorithm:ssh-ed25519-literal`, and `subject_schema_sha256:sha256`. The signature set exactly covers every named bootstrap/catalog/registry/provider-registration/caller-request/blob-request/authority-decision/append-result/blob-result/blob-resolve/lookup-result/registry-CAS/recovery/continuity/key-activation/source-revision/source-manifest/release namespace in this PRD, including `datarim-source-revision-v1`, `datarim-source-manifest-v1`, both genesis invocation/result/lookup namespace families, `datarim-key-continuity-v1`, and `datarim-key-activation-v1`; an unregistered namespace fails even under a valid key. A minimum-version record has exactly `profile:id128` and `minimum_version:uint`; recovery policy has exactly `recovery_signers[]`, `quorum:uint`, and `record_namespace:id128`. Each recovery signer has exactly `signer_id:id128`, `key_id:id128`, `fingerprint:text`, `public_key:text`, and `principal:id128`; records are canonical-byte sorted with distinct key IDs/fingerprints, `1 <= quorum <= signer count`, and a signature set counts each registered key at most once over exact canonical recovery-record bytes. The unsigned recovery record has exactly `schema_version:uint=1`, `recovery_id:id128`, `previous_registry_sha256:sha256`, `replacement_registry_preimage_sha256:sha256`, `compromised_key_ids[]:id128`, `replacement_key_ids[]:id128`, `compromised_governance_key_id:id128|null`, `replacement_governance_key` (a recovery-signer-shaped record or null), `key_activations[]`, `adopted_pending_activation_nonces[]:id128`, `reason:text`, and `issued_at:utc_seconds`; key arrays are non-empty, duplicate-free, disjoint, and exactly equal the removed/introduced key sets; `adopted_pending_activation_nonces[]` is canonical-byte sorted and duplicate-free and names only an already installed pending transition whose nonce and stable identity/old/replacement key/handle fields exactly equal one recovery activation, whose old active key is explicitly compromised, and whose pending-key possession is independently reproved by the recovery rebind below; the quorum thereby adopts that safe pending generation without relying on its earlier continuity signature or ordinary successor bytes. Each activation has exactly `identity_kind` (`caller`, `signer`, or `governance`), `identity_id:id128`, `provider_id:id128`, `old_key_id:id128`, `replacement_key_id:id128`, `replacement_principal:id128`, `replacement_fingerprint:text`, `replacement_public_key:text`, `replacement_secret_store_ref:locator`, and `activation_nonce:id128`; `old_key_id`/`replacement_key_id` activations bijectively map the two key arrays and stable identities/providers, and secret-store locators identify handles but contain no key bytes. `replacement_registry_preimage_sha256` hashes canonical successor-registry bytes with every newly added recovery revocation's `authority_id`, `authority_locator`, and `authority_sha256` set to null. Governance fields are both null or both non-null, and `recovery_id` is the SHA-256-derived ID of the canonical record with that field omitted. Its authorization bundle has exactly `record` and `signatures[]`; each signature is a detached envelope in the catalog's recovery namespace over the exact canonical record, signatures are sorted by registered key ID, and the distinct valid-key count must meet quorum. A non-compromise governance-root rotation uses a continuity bundle with exactly `record`, `old_signature`, and `new_signature`; its record has exactly `schema_version:uint=1`, `previous_catalog_sha256:sha256`, `replacement_catalog_sha256:sha256`, `identity_kind:governance-literal`, `identity_id:id128`, `provider_id:id128`, `old_key_id:id128`, `replacement_key_id:id128`, `replacement_principal:id128`, `replacement_fingerprint:text`, `replacement_public_key:text`, `replacement_secret_store_ref:locator`, `activation_nonce:id128`, and `issued_at:utc_seconds`, and both signatures cover those exact bytes under `datarim-governance-continuity-v1` with their respective old/new keys. An ordinary non-compromise caller/signer rotation uses a key-continuity bundle with exactly `record`, `old_signature`, and `new_signature`; its record has exactly `schema_version:uint=1`, `previous_registry_sha256:sha256`, `replacement_registry_sha256:sha256`, `identity_kind` (`caller` or `signer`), `identity_id:id128`, `provider_id:id128`, `old_key_id:id128`, `replacement_key_id:id128`, `replacement_principal:id128`, `replacement_fingerprint:text`, `replacement_public_key:text`, `replacement_secret_store_ref:locator`, `activation_nonce:id128`, and `issued_at:utc_seconds`. Each continuity record's activation fields have the exact common activation shape used below. The active old and proposed new keys both sign exact record bytes under `datarim-key-continuity-v1` using the distinct key-possession envelope shape defined below with matching identity and respective key/principal; the registry delta may change only that stable identity's key-dependent fields plus the old-key revocation. Consumer schema v1 has exactly `schema_version:uint=1`, `registry_id:id128`, `sequence:uint`, `previous_registry_sha256:sha256|null`, `project_id:id128`, `repository_id:id128`, `mode`, `canon_epoch`, `framework_catalog_sha256:sha256`, `allowed_signers_sha256:sha256`, `verifiers[]`, `callers[]`, `signers[]`, `providers[]`, `source_commitment_policy`, and `revocations[]`. Mode is `genesis`, `legacy`, `dual-read`, or `strict`; strict cannot revert. `canon_epoch` is null before genesis release or exactly `release_version:text`, `tag_object_oid:git_oid`, `peeled_commit_oid:git_oid`, `ledger_blob_oid:git_oid`, `ledger_head:sha256`, and `anchor_receipt_sha256:sha256`.

`activate-key` is the only normative private-key transition and is explicitly two-phase; `rebind` is a prepare-side recovery sub-operation and never activates a key. Its request has exactly `schema_version:uint=1`, `operation:activate-key-literal`, `action` (`prepare`, `rebind`, or `finalize`), `authorization_mode` (`continuity` or `recovery`), `previous_registry_locator:locator`, `previous_registry_sha256:sha256`, `replacement_registry_locator:locator`, `replacement_registry_sha256:sha256`, `authority_bundle_locator:locator`, `authority_bundle_sha256:sha256`, `activation`, `supersession`, `prepare_result_output:relative_path`, `prepare_proof_output:relative_path`, `cas_result_locator:locator|null`, `cas_result_sha256:sha256|null`, `cas_result_signature_locator:locator|null`, `cas_result_signature_sha256:sha256|null`, and `idempotency_key:sha256`. Activation has exactly `identity_kind`, `identity_id`, `provider_id`, `old_key_id`, `replacement_key_id`, `replacement_principal`, `replacement_fingerprint`, `replacement_public_key`, `replacement_secret_store_ref`, and `activation_nonce`; it must equal the projection from the authorized continuity record or one recovery activation. `supersession` is null for an initial prepare/rebind or has exactly `superseded_activation_nonce:id128`, `superseded_registry_cas_request_locator:locator`, `superseded_registry_cas_request_sha256:sha256`, `absence_lookup_locator:locator`, `absence_lookup_sha256:sha256`, `absence_lookup_signature_locator:locator`, and `absence_lookup_signature_sha256:sha256`. Prepare and rebind require all four CAS-result fields null; rebind additionally requires recovery mode, null supersession, and an already installed pending transition with the same activation nonce/identity/key/handle/generation. Finalize requires all four non-null and `supersession` byte-identical to its winning prepare, or null when the winning recovery tuple is a rebind. The idempotency key hashes the canonical request with only `idempotency_key` omitted, including supersession and both output paths; an identical key with any different request byte or output target fails.

Before touching a handle or slot, prepare creates/fsyncs `<registered private_state_root>/transitions/activate-key/<activation_nonce>.json` and its parent. The journal has exactly `schema_version:uint=1`, `operation:activate-key-literal`, `activation_nonce:id128`, `authorization_mode`, `prepare_request_sha256:sha256`, `rebind_request_locator:relative_path|null`, `rebind_request_sha256:sha256|null`, `rebind_authorization_mode:recovery-literal|null`, `rebind_authority_bundle_sha256:sha256|null`, `rebind_previous_registry_sha256:sha256|null`, `rebind_replacement_registry_sha256:sha256|null`, `rebind_result_locator:relative_path|null`, `rebind_result_sha256:sha256|null`, `rebind_proof_locator:relative_path|null`, `rebind_proof_sha256:sha256|null`, `finalize_request_sha256:sha256|null`, `supersession_sha256:sha256|null`, `authority_bundle_sha256:sha256`, `previous_registry_sha256:sha256`, `replacement_registry_sha256:sha256`, `identity_kind`, `identity_id`, `provider_id`, `old_key_id`, `replacement_key_id`, `old_slot_generation:uint`, `pending_slot_generation:uint|null`, `active_slot_generation:uint`, `state` (`prepared`, `pending`, `rebind-prepared`, `rebound`, `superseded`, `cas-accepted`, or `complete`), `supersedes_activation_nonce:id128|null`, `superseded_by_activation_nonce:id128|null`, `previous_handle_ref:locator`, `replacement_handle_ref:locator`, `prepare_result_locator:relative_path|null`, `prepare_result_sha256:sha256|null`, `prepare_proof_locator:relative_path|null`, `prepare_proof_sha256:sha256|null`, `cas_result_locator:locator|null`, `cas_result_sha256:sha256|null`, `cas_result_signature_locator:locator|null`, and `cas_result_signature_sha256:sha256|null`. Each atomic replace and its parent are fsynced. The six rebind reservation fields from request locator through replacement registry digest are all null or all non-null; the four rebind result/proof fields are all null or all non-null and may be non-null only when the reservation is non-null. The deterministic request copy is `<registered private_state_root>/transitions/activate-key/<activation_nonce>.rebind-request.json`; its create-exclusive/create-or-verify bytes and parent are fsynced before the journal enters `rebind-prepared`, so a crash cannot admit another request digest. `supersession_sha256` hashes the exact non-null supersession object; it and `supersedes_activation_nonce` are both null or both non-null, while `superseded_by_activation_nonce` is independently null until this journal is replaced. In `prepared`, the pending generation, both prepare artifact pairs, finalize digest, and all CAS fields are null and the active generation equals the old generation. In `pending`, the pending generation and both prepare artifact pairs are non-null, every rebind field, the finalize digest, and CAS fields are null, and the active generation remains old. In `rebind-prepared`, the reservation is non-null, result/proof are null, and retry must resume only the stored request. In `rebound`, every rebind field is non-null and finalize/CAS fields remain null. Both rebind states preserve exact old-active plus exact pending identity state. In `superseded`, `superseded_by_activation_nonce` is non-null, the finalize digest and CAS fields are null, the recorded prepared generation remains non-null but is no longer installed pending, and the active generation remains old. In `cas-accepted`, the pending generation, both prepare artifact pairs, finalize digest, and all CAS fields are non-null and the active generation remains old. In `complete`, those fields remain non-null and the active generation equals the prepared pending generation. No other nullability or transition is valid.

The one authority for active/pending selection is `<registered private_state_root>/identities/<identity_kind>/<identity_id>.json`. It has exactly `schema_version:uint=1`, `identity_kind`, `identity_id:id128`, `provider_id:id128`, `revision:uint`, `active_key_id:id128`, `active_handle_ref:locator`, `active_slot_generation:uint`, `pending_activation_nonce:id128|null`, `pending_key_id:id128|null`, `pending_handle_ref:locator|null`, and `pending_slot_generation:uint|null`; the four pending fields are all null or all non-null, generations never decrease, and revision increments exactly once per successful whole-document compare-and-swap. The identity-scoped lock covers byte-for-byte read, expected-document comparison, temporary-file fsync, atomic replacement, and parent fsync. Prepare compare-swaps `{revision, old active, pending null}` to `{revision+1, same active, exact pending}`; safe supersession compare-swaps `{revision, same active, exact old pending}` to `{revision+1, same active, exact new pending}`; finalize compare-swaps `{revision, exact old active, exact pending}` to `{revision+1, successor active, pending all null}`. A mismatched byte, handle, key, generation, provider, or identity fails closed before mutation. Genesis-trust create-exclusively initializes one document for every caller, signer, and governance identity before its key can be used: `revision=0`, the registered key/handle/provider are active, `active_slot_generation=0`, and all pending fields are null; file and parent are fsynced before the bootstrap result is signed. The governance document maps the pre-existing governance root's configured key-provider handle under the same rule, while every later governance-authorized provider registration must resolve an already initialized matching document or create-exclusively initialize a genuinely new identity with the same zero/null rules before acceptance; it cannot replace an existing identity or invent different initial values. The protected-ref provider registration likewise initializes its current authoritative registry tuple to the exact active signed consumer registry locator/sequence/digest before accepting a registry CAS.

Prepare verifies old+new continuity signatures or prior-catalog recovery quorum without consulting a compromised key, derives and matches the replacement handle's public key, and, under one identity-scoped lock, compare-swaps an empty pending slot to the new generation; the predecessor active slot and generation remain unchanged. A concurrent non-identical prepare against a non-empty slot fails without changing either journal or slot; an identical request is an idempotent replay. A fresh prepare may supersede a different pending request only with a non-null supersession object: prepare refetches its exact old registry-CAS request, signed absence lookup, and lookup signature; requires the target old-request activation tuple to match this identity's pending journal/state (other identities in the same CAS are checked only by their own serialized transition); verifies the lookup under the normal or transition-authorized signer rule; requires `activation_acceptances[]` empty; and requires the lookup's observed current registry locator/sequence/digest to differ from both the old request's expected registry tuple and its replacement registry tuple. Every CAS request containing that activation must carry the prepare's exact previous/replacement registry locator/digests and is valid only against that same predecessor, so this third-registry observation plus the complete ancestry-negative result permanently fences all delayed variants, not merely one idempotency key. If the current tuple still equals the expected tuple, absence is not terminal and cannot supersede. If any historical acceptance exists—including when the current tuple equals the prepared replacement or a later descendant—the lookup returns its introducing CAS and the activation finalizes instead of being superseded. Under the same identity lock, safe supersession first creates/fsyncs the new `prepared` journal with the old nonce in `supersedes_activation_nonce`, then compare-swaps the single authoritative identity-state document from the old pending nonce/generation to the new one, then marks/fsyncs the old journal `superseded` and the new journal `pending`. On restart the identity-state document is authoritative: an old slot leaves the new journal `prepared` and the old journal `pending`; a new slot deterministically completes both journal updates before any signing or other operation. Thus a crash can expose only the old or new pending slot, never both, and no journal-only intermediate state grants key authority. The prepare result has exactly `schema_version:uint=1`, `operation:activate-key-literal`, `action:prepare-literal`, `authorization_mode`, `identity_kind`, `identity_id`, `provider_id`, `old_key_id`, `replacement_key_id`, `replacement_fingerprint`, `activation_nonce`, `slot_lineage_sha256:sha256`, `request_sha256`, `authority_bundle_sha256`, `old_slot_generation:uint`, `pending_slot_generation:uint`, and `prepared:true-literal`. Its proof is a distinct key-possession envelope having exactly `schema_version:uint=1`, `payload_sha256:sha256`, `algorithm:ssh-ed25519-literal`, `namespace:id128`, `identity_kind`, `identity_id`, `key_id`, `principal`, and `sshsig_b64:text`; the pending replacement key signs exact prepare-result bytes and the prepare proof requires namespace `datarim-key-activation-v1`; continuity `old_signature`/`new_signature` use the identical envelope keys with namespace `datarim-key-continuity-v1` and their respective identity/key/principal. Result/proof are create-or-verify written, fsynced, then journaled `pending` before response. Identical prepare retry returns those byte-identical blobs.

Rebind acquires the same identity lock but never changes the identity-state document. It requires exact old-active plus exact pending state, verifies the prior-catalog recovery quorum without the compromised active key, requires the recovery record to adopt this nonce and match the installed stable identity/key/handle/generation, re-derives the pending public key, and binds the recovery predecessor/successor registry bytes. The slot-lineage record has exactly `activation_nonce:id128`, `identity_kind`, `identity_id:id128`, `provider_id:id128`, `old_key_id:id128`, `replacement_key_id:id128`, `old_slot_generation:uint`, `pending_slot_generation:uint`, `replacement_fingerprint:text`, `replacement_public_key:text`, and `replacement_handle_ref:locator`; the public key is the byte-identical validated activation JSON string (no trimming, newline insertion, comment removal, Unicode normalization, decoding, or re-encoding), and the handle is the byte-identical `replacement_secret_store_ref`. `slot_lineage_sha256` hashes the canonical JSON record bytes and must equal the value in both the original prepare result and rebind result. Its result has exactly `schema_version:uint=1`, `operation:activate-key-literal`, `action:rebind-literal`, `authorization_mode:recovery-literal`, `identity_kind`, `identity_id`, `provider_id`, `old_key_id`, `replacement_key_id`, `replacement_fingerprint`, `activation_nonce`, `slot_lineage_sha256:sha256`, `original_prepare_request_sha256`, `request_sha256`, `authority_bundle_sha256`, `old_slot_generation:uint`, `pending_slot_generation:uint`, and `rebound:true-literal`. Before producing either artifact, rebind create-exclusively/create-or-verifies and fsyncs the deterministic exact request copy, then atomically journals/fsyncs the six-field reservation in `rebind-prepared`. Only that stored request may resume. The already pending replacement key then signs exact rebind-result bytes in a fresh key-possession proof under `datarim-key-activation-v1`; result/proof are create-or-verify written and fsynced, then their four fields are atomically journaled/fsynced in `rebound` before response. A crash at either boundary resumes the stored request and byte-identical artifacts; a different recovery binding or output path for the same journal fails before signing. The old normal CAS and new recovery CAS may then race on the same registry predecessor: each is bound to its own successor and prepare/rebind tuple, exactly one registry CAS can win, and either winner finalizes the same pending handle/generation while making the loser stale.

Registry CAS retains and verifies the immutable prepare request, authority bundle, result, and proof for every changed key. The request's expected registry locator/sequence/digest is the activation fence: under the provider mutation lock both must equal the current authoritative registry tuple before control creation, each prepare/rebind's previous/replacement registry locator/digests must equal the request predecessor/successor, and one identity may appear at most once. Provider control traffic does not change that registry tuple and therefore cannot stale the signed request; a different accepted registry CAS does. In normal mode the still-active predecessor registry-owner signs the finalized CAS request and the still-active predecessor result signer signs the CAS result; their signatures are accepted only for this continuity transition because old+new continuity and pending-key proof authorize the exact successor. In recovery mode the prior-catalog quorum signs the finalized CAS request. A recovery activation may reuse an already installed ordinary pending generation only through its journaled recovery rebind: the recovery record's `adopted_pending_activation_nonces[]` names it, quorum verification matches the stable identity/handle-derived key, and the rebind's fresh pending-key proof binds the recovery successor. CAS uses that rebind tuple and recovery bundle, never the incompatible ordinary successor/continuity tuple. When recovery rotates the provider's fixed result signer, its pending successor key signs the CAS result only when its prepare proof and recovery activation match under either a recovery prepare or the journaled quorum-authorized rebind; otherwise the unchanged still-active predecessor-registry result signer signs it. No active slot changes inside CAS. When normal continuity or recovery rotates that fixed result signer, its pending successor key may sign only `lookup-provider-operation` for the exact registry-CAS request named by a non-null transition authorization. This covers both `absent` before CAS and `present` after CAS; verification refetches and binds the request authorization, continuity/recovery authority, prepare result/proof, observed protected head and current registry tuple, accepted control when present, and stable signer identity. It has no authority over another idempotency key or operation.

Finalize refetches the successful immutable CAS result/signature, verifies that the accepted successor locator/digest and activation object set equal either its original prepare tuple or its one journaled recovery rebind tuple, journals `cas-accepted`, then atomically compare-swaps the whole identity-state document from exact old active plus exact pending to successor active plus all pending fields null and fsyncs document, parent, and journal. Its result has exactly `schema_version:uint=1`, `operation:activate-key-literal`, `action:finalize-literal`, `authorization_mode`, `identity_kind`, `identity_id`, `provider_id`, `old_key_id`, `replacement_key_id`, `activation_nonce`, `cas_result_sha256`, `previous_slot_generation:uint`, `active_slot_generation:uint`, and `activated:true-literal`; the finalize wire has exactly `result`, while prepare and rebind each have exactly `result` and `proof`. The winning CAS authorization mode/bundle/result tuple is recorded; the losing normal or recovery request is thereafter stale, but both winners promote the same pending generation. A crash resumes from the journal: old-active plus exact pending resumes before compare-swap; exact successor-active plus pending all null after a completed compare-swap deterministically writes `complete` and re-emits the same result; any other handle/generation/lineage combination fails closed. A stale/failed CAS leaves the predecessor slot active; only the terminal different-registry absence proof above permits safe supersession. A compromised predecessor is never reactivated, and no failed CAS can strand the provider on an unaccepted key.

A verifier record has exactly `profile:id128`, `platform:id128`, `executable:relative_path`, `version:text`, `executable_sha256:sha256`, and `algorithm:id128`. A caller record has exactly `caller_subject:id128`, `key_id:id128`, `fingerprint:text`, `public_key:text`, `principal:id128`, `caller_provider_id:id128`, `authority_provider_id:id128`, `signer_id:id128`, `namespaces[]:id128`, `operation_scopes[]:id128`, `valid_from:utc_seconds`, `valid_until:utc_seconds|null`, and `status` (`active` or `revoked`). The pair `(authority_provider_id, caller_subject)` is unique and resolves exactly one active caller, its independently registered `caller-sign-v1` provider, and one authority signer; the signer reciprocally names the caller subject/provider/key holder. A signer record has exactly `signer_id:id128`, `key_id:id128`, `fingerprint:text`, `public_key:text`, `identity:id128`, `role:authority_role`, `provider_id:id128`, `caller_provider_id:id128`, `authorization_policy_locator:locator`, `authorization_policy_sha256:sha256`, `caller_subject:id128`, `receipt_types[]:id128`, `namespaces[]:id128`, `repository_scopes[]:id128`, `project_scopes[]:id128`, `task_scopes[]:id128`, `requirement_scopes[]:id128`, `valid_from:utc_seconds`, `valid_until:utc_seconds|null`, `predecessor_key_id:id128|null`, and `status` (`active` or `revoked`). A provider record has exactly `provider_id:id128`, `provider_type:id128`, `executable:relative_path`, `version:text`, `executable_sha256:sha256`, `cwd:relative_path`, `argv_schema_locator:locator`, `argv_schema_sha256:sha256`, `environment_allowlist[]:id128`, `timeout_minutes:uint`, `output_cap_bytes:uint`, `result_schema_locator:locator`, `result_schema_sha256:sha256`, `result_signer_id:id128|null`, `authentication_subject:id128`, `authentication_audience:id128`, `repository_id:id128|null`, `remote_url:https_url|null`, `protected_ref:text|null`, `ruleset_id:uint|null`, `ruleset_sha256:sha256|null`, `bootstrap_registration_locator:locator`, `bootstrap_registration_sha256:sha256`, `bootstrap_registration_signature_locator:locator`, `bootstrap_registration_signature_sha256:sha256`, `registration_locator:locator`, `registration_sha256:sha256`, `registration_signature_locator:locator`, and `registration_signature_sha256:sha256`; Git authorities require every nullable host field and `result_signer_id` non-null, caller/semantic signer providers require every nullable host field to be null and `result_signer_id` null. Every non-null result signer resolves to exactly one active signer whose role is `anchor-authority`, whose `provider_id` reciprocally equals this provider, and whose namespace/scope set covers every registered result operation; no provider selects among signers. `timeout_minutes` is an integer from 1 through 60. The separately stored provider-registration payload has exactly the same fields except the four `registration_*` locator/digest fields; it retains the four `bootstrap_registration_*` fields and thereby binds the exact pre-registry bootstrap payload/signature tuple without self-reference. It is canonicalized and signed directly by governance under namespace `datarim-provider-registration-v1`. The registry independently binds both bootstrap blobs and both provider-registration blobs; resolve retrieves, hashes, and verifies all four exact blobs before use. Source-commitment policy has exactly `profile:datarim-source-hmac-sha256-v1-literal`, `active_key_ids[]:id128`, and `historical_key_retention:retain-encrypted-indefinitely-literal`. A revocation record has exactly `sequence:uint`, `previous_sha256:sha256|null`, `key_id:id128`, `effective_registry_sequence:uint`, `reason:text`, `authority_mode` (`normal` or `recovery`), `authority_id:id128|null`, `authority_locator:locator|null`, and `authority_sha256:sha256|null`; the key must exist in the predecessor registry and `effective_registry_sequence` must equal the containing successor registry sequence. Normal mode requires `authority_id` equal the authorized continuity record's `activation_nonce` and requires locator/digest null. The verifier retrieves the exact ordinary key-continuity or governance-continuity bundle solely from the immutable registry-CAS prepare/control tuple, checks both old/new signatures, exact predecessor/successor digests, stable identity, key delta, and nonce, and then matches this revocation; no successor field contains the bundle digest, so ordinary rotation has no cryptographic cycle. It is not a receipt and no receipt-subject enum is involved. Recovery mode requires `authority_id` equal the quorum record's `recovery_id` and both locator/digest null; the verifier retrieves the bundle from the immutable registry-CAS control/authorization tuple, nulls those three fields in every newly added recovery revocation, recomputes `replacement_registry_preimage_sha256`, verifies the recovery ID/quorum, and then verifies the separate quorum-signed recovery-CAS authorization over the final request/final successor digest. No successor field contains the bundle digest, so no cryptographic fixed point exists. All set-like arrays are canonical-byte sorted and duplicate-free. Unknown keys or enum values fail. The allowed-signers view digest must match the consumer registry and each line must reproduce every active signer and caller namespace without extra principals or namespaces.

Catalog and consumer signatures use distinct namespaces `datarim-trust-catalog-v1` and `datarim-consumer-registry-v1`. A key introduced by a consumer registry cannot authenticate that same registry. Genesis pins the governance SSH fingerprint `SHA256:a3hhUtadjU+KNZ1zI2LQuS88vb+t7YnQUiA4IDNbLgM` outside both artifacts in the reviewed bootstrap; verification first recomputes that fingerprint from the governance public key, verifies the catalog, then verifies the consumer registry against the catalog and predecessor. Ambient Git trust cannot replace this chain. Before freeze, TUNE-0585 provisions distinct private signer identities in the configured non-repository secret store and commits only public entries for `registry-owner`, `spec-reviewer`, `atomicity-reviewer`, `evolution-owner`, `evolution-reviewer`, `quality-reviewer`, `evidence-runner`, `project-verifier`, `source-provider`, `deployment-provider`, `visual-provider`, `reconciliation-authority`, `anchor-authority`, and `release-authority`, plus a disjoint offline recovery quorum whose keys are not held by any ordinary provider process; governance remains the pre-existing root and GitHub's protected-ref API is the registered anchor provider. The consumer registry fixes the permanent receipt branch/provider locator before any event attestation or final review. No key is reused across mutually exclusive roles or recovery/ordinary authority, and the implementer/runner cannot hold registry-owner, reviewer, reconciliation, anchor, release, or recovery keys in the respective process. Customer keys are supplied only by a consumer registration and cannot be synthesized by the framework. Private keys, HMAC keys, provider credentials, recovery keys, and customer identities never enter Git.

Every receipt uses canonical schema-v1 payload plus a detached signature envelope. The common payload has exactly `schema_version:uint=1`, `receipt_type:id128`, `receipt_id:id128`, `profile:id128`, `registry_id:id128`, `registry_sequence:uint`, `registry_sha256:sha256`, `canon_epoch` (the exact consumer-record shape or null), `project_id:id128`, `repository_id:id128`, `task_id:id128`, `requirement_id:id128|null`, `subject_sha256:sha256`, `subject`, `issued_at:utc_seconds`, and `idempotency_key:sha256`; fields that do not apply are explicit null, never omitted. Provider publication order is deliberately not inside authority-signed semantic receipt bytes. The detached JSON signature envelope has exactly `schema_version:uint=1`, `payload_sha256:sha256`, `algorithm:ssh-ed25519-literal`, `namespace:id128`, `signer_id:id128`, `key_id:id128`, `principal:id128`, and `sshsig_b64:text`; Base64 is RFC 4648 standard alphabet with padding/no whitespace and decodes to one OpenSSH SSHSIG over the canonical payload bytes.

Caller proofs use a distinct envelope and can never be decoded as authority signatures. It has exactly `schema_version:uint=1`, `payload_sha256:sha256`, `algorithm:ssh-ed25519-literal`, `namespace:id128`, `caller_subject:id128`, `key_id:id128`, `principal:id128`, and `sshsig_b64:text`; these identity fields equal the unique active caller record. It contains no `signer_id`. Every reference to a caller signature or caller proof in this PRD means this envelope; the common detached envelope above is reserved for authority/result/governance signers.

Receipt subjects are closed. `event-attestation` has exactly `event_id:id128`, `event_index:uint`, `event_hash:sha256`, and `ledger_preimage_sha256:sha256`; `source-manifest` has `manifest_id:id128`, `manifest_head:sha256`, `item_count:uint`, `exhausted:boolean`, `pagination_cursor_sha256:sha256`, and `ordered_commitment_sha256:sha256`; `atomicity-review` has `source_manifest_sha256:sha256`, `requirement_graph_sha256:sha256`, `mapping_cardinality` (the exact event shape), `predecessor_head:sha256`, `spec_revision:git_oid`, and `verdict`; `spec-review` has `reviewed_pair_commit:git_oid`, `prd_blob_oid:git_oid`, `plan_blob_oid:git_oid`, `trust_provision_commit:git_oid`, `finding_sha256:sha256`, and `verdict`; `evolution-approval` has `request_id:id128`, `requested_event_hash:sha256`, `proposal_sha256:sha256`, and `decision:approved-literal`; `evolution-rejection` has `request_id:id128`, `requested_event_hash:sha256`, `proposal_sha256:sha256`, and `reason:text`; `evolution-decline` has `request_id:id128`, `rejected_event_hash:sha256`, and `terminal:true`; `evolution-review` has `request_id:id128`, `artifact_after_sha256:sha256`, `reviewed_event_hash:sha256`, and `verdict`; `quality-review` has `commit_oid:git_oid`, `tree_oid:git_oid`, `ledger_blob_oid:git_oid`, `ledger_head:sha256`, `event_count:uint`, `evidence_manifest_sha256:sha256`, `finding_sha256:sha256`, and `verdict`; `runner-red` and `runner-green` have `assertion_id:id128`, `argv[]` (ordered strings), `tree_sha256:sha256`, `mutation_sha256:sha256|null`, `output_sha256:sha256`, `result_exit_code:int`, `raw_wait_status:uint|null`, `timed_out:boolean`, `termination_signal:uint|null`, `forced_kill:boolean`, `descendants_survived:boolean`, and `evidence_path:relative_path`; `project-verification` has `requirement_id:id128`, `red_receipt_sha256:sha256`, `green_receipt_sha256:sha256`, and `disposition:verified-literal`; `customer-disposition` has `authority_record_id:id128`, `requirement_id:id128`, `deployed_sha:git_oid`, `disposition`, `private_commitment:sha256`, and `private_locator:locator`; `deployment` has `requirement_id:id128`, `source_sha:git_oid`, `deployed_sha:git_oid`, and `provenance_sha256:sha256`; `visual` has exactly the `visual_observation` payload keys except `authority` and `provider_receipt`; `authorization` has `freeze_commit:git_oid`, `repository_id:id128`, `clean_base_sha:git_oid`, `marker_commit:git_oid`, and `marker_sha256:sha256`; `reconciliation` has `conflicts[]:conflict_ref` and `chosen_successor_event_id:id128`; `anchor` has `event_head:sha256`, `event_count:uint`, `ledger_blob_oid:git_oid`, `byte_length:uint`, `provider_ref:text`, `provider_commit_oid:git_oid`, and `previous_receipt_sha256:sha256|null`; `provider-registration` has `provider_id:id128`, `registration_locator:locator`, and `registration_sha256:sha256`; `migration-recovery` has `plan_sha256:sha256`, `transition_index:uint`, `phase:id128`, `receipt_state_sha256:sha256`, `ledger_sha256:sha256`, `target_installed:boolean`, `anchor_receipt_sha256:sha256|null`, and `registry_sha256:sha256|null`; `release-tag` has `release_version:text`, `reviewed_pr_head:git_oid`, `merged_commit_oid:git_oid`, `tree_oid:git_oid`, `ledger_path:relative_path`, `ledger_blob_oid:git_oid`, `ledger_byte_length:uint`, `event_count:uint`, `ledger_head:sha256`, `quality_receipt_locator:locator`, `quality_receipt_sha256:sha256`, `ruleset_id:uint`, `ruleset_sha256:sha256`, `verifier_profile:id128`, `verifier_version:text`, and `verifier_executable_sha256:sha256`. Each receipt type has one fixed catalog namespace. Wrong class, role, namespace, scope, registry revision, append-result provider sequence, or subject binding fails even under a valid key. `config/customer-delivery-receipt-v1.schema.json`, `config/customer-delivery-trust-catalog-v1.schema.json`, and `config/customer-delivery-project-v1.schema.json` are exact executable transcriptions, not extensible policy.

Genesis has one explicit, irreducible bootstrap boundary rather than pretending the framework can self-authorize code that does not yet exist. After the exact PRD/plan pair receives two independent `SPEC READY: YES` verdicts, governance directly signs `datarim/delivery/TUNE-0585.bootstrap-authorization.json` under namespace `datarim-bootstrap-authorization-v1`. That closed object has exactly `schema_version:uint=1`, `reviewed_pair_commit:git_oid`, `clean_base_sha:git_oid`, `allowed_source_paths[]:relative_path`, `allowed_test_paths[]:relative_path`, `role_refs[]:artifact_ref`, `skill_refs[]:artifact_ref`, `blueprint_refs[]:artifact_ref`, `constraint_refs[]:artifact_ref`, `policy_refs[]:artifact_ref`, `success_criterion_refs[]:artifact_ref`, and `authorization_expires_at:utc_seconds`; every capability array is non-empty, paths are exact/non-overlapping, and the blueprint is the reviewed pair. Only the bootstrap test, executable, and `config/customer-delivery-bootstrap-wire-v1.schema.json` paths are authorized; that reviewed wire document is the executable closed transcription of every bootstrap request, result, signature namespace, policy record, and provider-registration-manifest shape required before trust. A signed RED commit must precede the bootstrap source commit, and a separate exact-head subordinate quality report plus governance-signed bootstrap-review record must pass before the executable can provision any trust. The bootstrap-review record has exactly `schema_version:uint=1`, `reviewed_pair_commit:git_oid`, `bootstrap_authorization_locator:locator`, `bootstrap_authorization_sha256:sha256`, `bootstrap_authorization_signature_locator:locator`, `bootstrap_authorization_signature_sha256:sha256`, `bootstrap_commit:git_oid`, `bootstrap_tree:git_oid`, `bootstrap_executable_sha256:sha256`, `bootstrap_wire_schema_sha256:sha256`, `bootstrap_test_sha256:sha256`, `red_evidence_sha256:sha256`, `reviewer_report_locator:locator`, `reviewer_report_sha256:sha256`, `finding_sha256:sha256`, and `verdict` (`pass` or `fail`). Governance signs its exact canonical bytes under the distinct namespace `datarim-bootstrap-review-v1`; the detached envelope is stored beside it and is not a common receipt. This narrow zero-to-one ceremony is the only pre-canon exception; its authorization, RED, source, review, signatures, and signed commits are preserved and later rechecked by the canonical runner. No other implementation source may use it as precedent.

The reviewed bootstrap executable is then registered in `config/customer-delivery-bootstrap-provider-v1.json` plus its detached governance signature. That closed record has exactly `schema_version:uint=1`, `registration_id:id128`, `reviewed_pair_commit:git_oid`, `bootstrap_authorization_locator:locator`, `bootstrap_authorization_sha256:sha256`, `bootstrap_authorization_signature_locator:locator`, `bootstrap_authorization_signature_sha256:sha256`, `bootstrap_review_locator:locator`, `bootstrap_review_sha256:sha256`, `bootstrap_review_signature_locator:locator`, `bootstrap_review_signature_sha256:sha256`, `bootstrap_commit:git_oid`, `bootstrap_executable:relative_path`, `bootstrap_executable_sha256:sha256`, `bootstrap_wire_schema_locator:locator`, `bootstrap_wire_schema_sha256:sha256`, `governance_result_signer_ref:locator`, `python_executable:text`, `python_version:text`, `python_sha256:sha256`, `ssh_keygen_executable:text`, `ssh_keygen_version:text`, `ssh_keygen_sha256:sha256`, `git_executable:text`, `git_version:text`, `git_sha256:sha256`, `gh_executable:text`, `gh_version:text`, `gh_sha256:sha256`, `environment_allowlist[]:id128`, `working_directory:relative_path`, `private_state_root:locator`, and `command_templates[]`. Each command template has exactly `operation:id128`, `argv[]` (ordered text), `stdin_profile:id128|null`, `request_schema_locator:locator`, `request_schema_sha256:sha256`, `result_schema_locator:locator`, `result_schema_sha256:sha256`, and `expected_exit_codes[]:uint`; the exact operation set is `canonicalize`, `caller-sign`, `activate-key`, `genesis-trust`, `genesis-input`, `genesis-plan`, `genesis-prepare`, `genesis-attest`, `genesis-bundle`, `authorize-and-sign`, `publish-blob`, `resolve-blob`, `lookup-provider-operation`, `append-receipt`, `lookup-receipt`, `resolve-receipt`, and `registry-cas`. Every command-template schema locator is an exact `git-object:<bootstrap_commit>:config/customer-delivery-bootstrap-wire-v1.schema.json#<json-pointer>` locator and its digest is the canonical SHA-256 of that closed subschema; dangling or later Task 3 schema locators fail registration. The reviewed wire document also closes the provider-registration-manifest and bootstrap policy shapes. `private_state_root` is a pre-existing durable non-repository store with create-exclusive, atomic-replace, file-fsync, and directory-fsync support; every transition/probe journal resolves beneath it and never contains secret bytes. The fixed `governance_result_signer_ref` identifies the pre-existing non-repository governance key process and may sign only bootstrap transition/results named here. The detached signature envelope uses namespace `datarim-bootstrap-provider-v1` and the already pinned governance key directly; it is not a common receipt and requires no not-yet-existing consumer registry. The executable is standard-library Python, invokes only the registered system executables argv-only, implements the closed bootstrap wires in this PRD, and is fixed by commit/digest before trust/receipt use. Every genesis provider record binds this registration locator/digest. After the general implementation exists, differential tests require byte-identical outputs for every bootstrap vector; the bootstrap remains retained read-only as genesis evidence and is never silently replaced.

Bootstrap command requests and results are closed. `canonicalize` accepts exactly `schema_version:uint=1`, `operation:canonicalize-literal`, `profile:id128`, `input_locator:locator`, and `input_sha256:sha256`; it returns exactly `schema_version:uint=1`, `operation:canonicalize-literal`, `profile:id128`, `input_sha256:sha256`, `canonical_locator:locator`, `canonical_sha256:sha256`, and `byte_length:uint`. `caller-sign` accepts exactly `schema_version:uint=1`, `operation:caller-sign-literal`, `caller_subject:id128`, `caller_provider_id:id128`, `authority_provider_id:id128`, `namespace:id128`, `subject_sha256:sha256`, and `subject_b64:text`; it returns exactly `schema_version:uint=1`, `operation:caller-sign-literal`, `caller_subject:id128`, `caller_provider_id:id128`, `authority_provider_id:id128`, `namespace:id128`, `subject_sha256:sha256`, and `signature` (the exact caller-proof envelope). It decodes and hashes exact subject bytes, requires the namespace and operation to be allowed by the unique active caller record, and signs in the independently registered caller-key process; the caller cannot choose a different key/provider/signer. `genesis-trust` never accepts an unsigned commit selector. Its request is first governance-signed and committed. Governance then signs a separate invocation-authorization record having exactly `schema_version:uint=1`, `operation:genesis-trust-literal`, `allowed_actions[]` (the exact sorted set `apply`, `lookup`), `request_commit:git_oid`, `request_locator:relative_path`, `request_sha256:sha256`, `request_signature_locator:relative_path`, `request_signature_sha256:sha256`, `reviewed_bootstrap_commit:git_oid`, `issued_at:utc_seconds`, and `authorization_id:id128`; `authorization_id` is the SHA-256-derived ID of canonical authorization bytes with that field omitted. That authorization and its direct governance signature under `datarim-genesis-trust-invocation-v1` are committed in a later signed `trust-invocation` commit. The runtime invocation has exactly `schema_version:uint=1`, `operation:genesis-trust-literal`, `action` (`apply` or `lookup`), `authorization_commit:git_oid`, `authorization_locator:relative_path`, `authorization_sha256:sha256`, `authorization_signature_locator:relative_path`, and `authorization_signature_sha256:sha256`. It resolves the authorization/signature from `authorization_commit`, then resolves request/signature only from the authorization's exact `request_commit`; every digest and both detached governance signatures must verify. The authorization commit must have exactly one parent equal to the request commit, its tree delta must contain only the authorization/signature paths, and its Git commit signature must verify directly against the same pinned governance fingerprint; a merge, later descendant, ambient allowed-signers entry, or any other signer fails. The request commit is therefore authenticated before it becomes part of any policy locator or result. Copying identical request bytes to another commit is unauthorized. Idempotency is exactly `request_sha256`; neither request nor authorization self-names its containing commit.

The genesis-trust request has exactly `schema_version:uint=1`, `operation:genesis-trust-literal`, `reviewed_pair_commit:git_oid`, `bootstrap_authorization_locator:locator`, `bootstrap_authorization_sha256:sha256`, `bootstrap_review_locator:locator`, `bootstrap_review_sha256:sha256`, `recorded_at:utc_seconds`, `result_output:relative_path`, `result_signature_output:relative_path`, `catalog_parameters`, `registry_parameters`, `policy_specs[]`, `caller_specs[]`, `signer_specs[]`, `recovery_specs[]`, and `provider_specs[]`. Catalog parameters have exactly the catalog fields except `recovery_policy`; instead they contain `recovery_quorum:uint`, `recovery_namespace:id128`, and `recovery_identity_ids[]:id128`. Registry parameters have exactly `registry_id`, `sequence`, `previous_registry_sha256`, `project_id`, `repository_id`, `mode`, `canon_epoch`, `verifiers[]`, and `source_commitment_policy`; all use the already closed consumer field shapes. A policy spec has exactly `policy_id:id128`, `policy_b64:text`, and `policy_sha256:sha256`; decoded canonical JSON must satisfy the reviewed bootstrap policy subschema, and generated signer locators point to the immutable request commit plus that policy's JSON Pointer. A caller spec has exactly `caller_subject`, `key_id`, `principal`, `secret_store_ref`, `caller_provider_id`, `authority_provider_id`, `signer_id`, `namespaces[]`, `operation_scopes[]`, `valid_from`, `valid_until`, and `status`, using the caller-record types; fingerprint/public key are derived from the create-once private handle. A signer spec has exactly `signer_id`, `key_id`, `identity`, `role`, `provider_id`, `caller_provider_id`, `caller_subject`, `secret_store_ref`, `authorization_policy_id`, `receipt_types[]`, `namespaces[]`, `repository_scopes[]`, `project_scopes[]`, `task_scopes[]`, `requirement_scopes[]`, `valid_from`, `valid_until`, `predecessor_key_id`, and `status`, using the signer-record types; fingerprint/public key are derived. A recovery spec has exactly `signer_id:id128`, `key_id:id128`, `principal:id128`, and `secret_store_ref:locator`; it has no ordinary authority role/provider/caller and yields only the catalog recovery-signer public shape. Every ID/handle is unique where required and every set-like array is canonical-byte sorted and duplicate-free.

Each provider spec has exactly `provider_id`, `provider_type`, `executable`, `version`, `executable_sha256`, `cwd`, `argv_schema_locator`, `argv_schema_sha256`, `environment_allowlist`, `timeout_minutes`, `output_cap_bytes`, `result_schema_locator`, `result_schema_sha256`, `result_signer_id`, `authentication_subject`, `authentication_audience`, `repository_id`, `remote_url`, `protected_ref`, `ruleset_target`, `required_rules[]`, `required_bypass_count`, `registration_output`, and `registration_signature_output`, using provider-record types where shared. Every schema locator resolves inside the reviewed bootstrap wire document. Git providers require all host fields, one stable result signer ID, the exact sorted rules `deletion` and `non_fast_forward`, and zero bypass; non-Git providers require all host/ruleset fields null. Host-assigned ruleset ID/digest appear only in the result and generated provider records. The provider-registration manifest has exactly `schema_version:uint=1`, `request_commit:git_oid`, `request_sha256:sha256`, `bootstrap_registration_locator:locator`, `bootstrap_registration_sha256:sha256`, `bootstrap_registration_signature_locator:locator`, `bootstrap_registration_signature_sha256:sha256`, and `registrations[]`; each canonical-byte-sorted registration has exactly `provider_id`, `registration_locator`, `registration_sha256`, `registration_signature_locator`, and `registration_signature_sha256`. No implementation discretion remains for caller/signer scopes, validity, policies, provider registration paths, or manifest bytes.

Before any key, file, ref, or ruleset mutation, `genesis-trust apply` creates and fsyncs the private journal at `<registered private_state_root>/transitions/genesis-trust/<request_sha256>.json`, then fsyncs its parent store. The root comes only from the signed bootstrap registration and the child name comes only from the already computed request digest, so no request field or hash fixed point exists. The journal has exactly `schema_version:uint=1`, `operation:genesis-trust-literal`, `authorization_commit:git_oid`, `authorization_sha256:sha256`, `request_commit:git_oid`, `request_locator:relative_path`, `request_sha256:sha256`, `idempotency_key:sha256`, `state` (`prepared`, `applying`, or `complete`), `steps[]`, `result_locator:relative_path|null`, `result_sha256:sha256|null`, `result_signature_locator:relative_path|null`, and `result_signature_sha256:sha256|null`; each step has exactly `name` (`key-create`, `public-artifact`, `protected-ref`, `ruleset`, or `signed-result`), `subject_id:id128`, `status` (`planned` or `complete`), `resource_ref:locator`, and `public_sha256:sha256|null`. All intended handles, output paths, protected ref, and deterministic ruleset target are journaled in `planned` state before the first side effect; after each create/read-back the exact step update and directory are atomically replaced and fsynced. Key handles are create-once and carry the request digest ownership marker; retry reuses and verifies their public bytes, while a different request digest at any handle fails. The protected ref/ruleset use deterministic request-derived identity, create-or-verify exact semantics, and list/read-back reconciliation for response loss; a foreign or non-exact existing resource fails. Apply with identical authorization/request bytes resumes; altered bytes under the same resource identity fail. Partial failure remains resumable from the durable journal and cannot be relabeled as trust provision.

`genesis-trust lookup` is read-only and has one closed discriminated response. The lookup payload has exactly `schema_version:uint=1`, `operation:genesis-trust-lookup-literal`, `authorization_sha256:sha256`, `request_sha256:sha256`, `status` (`absent`, `in-progress`, or `complete`), `current_journal_sha256:sha256|null`, `journal_state:id128|null`, `last_completed_step:id128|null`, `transition_preimage_sha256:sha256|null`, `result_locator:relative_path|null`, `result_sha256:sha256|null`, `result_signature_locator:relative_path|null`, and `result_signature_sha256:sha256|null`. Absent requires all eight nullable fields null. In-progress requires current journal digest and state (`prepared` or `applying`) non-null, permits a null last step immediately after journal creation, requires transition preimage null until every non-result step is complete and non-null afterward, and requires all four result fields null. Complete requires current journal digest, state `complete`, last step `signed-result`, transition preimage, and all result fields non-null. The wire response has exactly `lookup` and `signature`; the fixed pre-existing governance result signer signs canonical lookup bytes under `datarim-genesis-trust-lookup-v1`. The bootstrap wire document registers a discriminated result union: action `apply` returns only the completed `{result, signature}` response, while action `lookup` returns only this signed lookup response. A complete lookup resolves and returns the byte-identical persisted result/signature by the four locators/digests; it never exposes private journal bytes.

The completed genesis-trust result has exactly `schema_version:uint=1`, `operation:genesis-trust-literal`, `authorization_commit:git_oid`, `authorization_sha256:sha256`, `request_commit:git_oid`, `request_locator:relative_path`, `request_sha256:sha256`, `idempotency_key:sha256`, `transition_preimage_sha256:sha256`, `catalog_locator:relative_path`, `catalog_sha256:sha256`, `catalog_signature_locator:relative_path`, `catalog_signature_sha256:sha256`, `registry_locator:relative_path`, `registry_sha256:sha256`, `registry_signature_locator:relative_path`, `registry_signature_sha256:sha256`, `allowed_signers_locator:relative_path`, `allowed_signers_sha256:sha256`, `provider_registration_manifest_locator:relative_path`, `provider_registration_manifest_sha256:sha256`, `protected_ref:text`, `ruleset_id:uint`, and `ruleset_sha256:sha256`. `transition_preimage_sha256` hashes the canonical journal in `applying` state after every non-result step is complete with all four result locator/digest fields null; the later complete journal may bind the result/signature digests without circularity. The wire response has exactly `result` and `signature`; the fixed pre-existing governance result signer signs canonical result bytes under `datarim-genesis-trust-result-v1`, and both exact blobs are atomically persisted and journaled before any response. Only a complete, request-bound, verified response may populate the signed `trust-provision` commit.

`genesis-input` uses the same two-commit authorization pattern, never the trust operation's authorization bytes. Its governance-signed invocation-authorization record has exactly `schema_version:uint=1`, `operation:genesis-input-literal`, `allowed_actions[]` (exactly `apply`, `lookup`), `request_commit:git_oid`, `request_locator:relative_path`, `request_sha256:sha256`, `request_signature_locator:relative_path`, `request_signature_sha256:sha256`, `issued_at:utc_seconds`, and `authorization_id:id128`, with the same derived-ID rule, and is signed under `datarim-genesis-input-invocation-v1` before the separate signed `attestation-input-authorization` commit. Runtime invocation has exactly the trust invocation fields with operation `genesis-input` and resolves authorization/signature from that authorization commit, then request/signature only from the authenticated request commit. Copying request bytes to another commit is unauthorized.

The genesis-input request has exactly `schema_version:uint=1`, `operation:genesis-input-literal`, `task_id:id128`, `project_id:id128`, `contract_manifest_locator:locator`, `contract_manifest_sha256:sha256`, `freeze_commit:git_oid`, `freeze_locator:locator`, `freeze_sha256:sha256`, `authorization_request_commit:git_oid`, `authorization_receipt_locator:locator`, `authorization_receipt_sha256:sha256`, `source_provider_id:id128`, `source_revision_locator:relative_path`, `source_revision_sha256:sha256`, `source_revision_signature_locator:relative_path`, `source_revision_signature_sha256:sha256`, `source_revision`, `registry_locator:locator`, `registry_sha256:sha256`, `recorded_at:utc_seconds`, `receipt_output:relative_path`, `result_output:relative_path`, and `result_signature_output:relative_path`. Source revision has exactly `schema_version:uint=1`, `source_provider_id:id128`, `provider_sequence:uint`, `provider_head:sha256`, `manifest_locator:relative_path`, `manifest_sha256:sha256`, `manifest_signature_locator:relative_path`, `manifest_signature_sha256:sha256`, `item_count:uint`, and `exhausted:true-literal`. Before request construction, the registry-fixed source provider's read-only observation profile emits the exhausted manifest/signature and revision/signature. All four source blobs plus request/signature are co-committed in the request commit; the revision signature verifies under `datarim-source-revision-v1`, the manifest signature verifies under `datarim-source-manifest-v1`, all relative locators resolve only from that authenticated request commit, and inline revision bytes equal the revision locator/digest. The immutable historical resolver must return that exact sequence/head/manifest; current source state can never substitute.

Before resolving or writing outputs, `genesis-input apply` creates/fsyncs `<registered private_state_root>/transitions/genesis-input/<request_sha256>.json` and its parent. Its journal has exactly the trust journal common fields with operation `genesis-input`, and steps use exactly `source-resolve`, `output-write`, or `signed-result`; intended output paths/digests are planned before writes, each file is create-or-verify exact, and every update is atomically replaced and file/directory-fsynced. The result has exactly `schema_version:uint=1`, `operation:genesis-input-literal`, `authorization_commit:git_oid`, `authorization_sha256:sha256`, `request_commit:git_oid`, `request_locator:relative_path`, `request_sha256:sha256`, `transition_preimage_sha256:sha256`, `source_revision_sha256:sha256`, `migration_input_locator:relative_path`, `migration_input_sha256:sha256`, `source_snapshot_locator:relative_path`, `source_snapshot_sha256:sha256`, `source_item_count:uint`, and `event_count:uint`. Its transition preimage uses the same noncircular applying-state rule. The apply wire has exactly `result` and the fixed bootstrap governance result signature under `datarim-genesis-input-result-v1`; both are persisted/journaled before response.

`genesis-input lookup` returns exactly `lookup` and `signature`; lookup has the same closed absent/in-progress/complete fields and nullability as genesis-trust lookup with operation `genesis-input-lookup`, and governance signs it under `datarim-genesis-input-lookup-v1`. Complete resolves the byte-identical result/signature; partial output state remains in-progress and identical apply safely resumes. Changed source revision, authorization, request, or pre-existing output bytes fail. The bootstrap wire registers the apply/lookup discriminated union and crash tests at journal creation, source resolution, each output write, result write, and response loss.

`genesis-plan` accepts exactly `schema_version:uint=1`, `operation:genesis-plan-literal`, `migration_input_locator:locator`, `migration_input_sha256:sha256`, `source_snapshot_locator:locator`, `source_snapshot_sha256:sha256`, `registry_locator:locator`, `registry_sha256:sha256`, `authorization_request_commit:git_oid`, `recorded_at:utc_seconds`, and `receipt_output:relative_path`; it returns exactly `schema_version:uint=1`, `operation:genesis-plan-literal`, `plan_locator:relative_path`, `plan_sha256:sha256`, `event_count:uint`, `ledger_byte_length:uint`, `ledger_sha256:sha256`, and `ledger_head:sha256`. `genesis-prepare` accepts exactly `schema_version:uint=1`, `operation:genesis-prepare-literal`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `registry_locator:locator`, and `registry_sha256:sha256`; it returns exactly `schema_version:uint=1`, `operation:genesis-prepare-literal`, `requests_manifest_locator:relative_path`, `requests_manifest_sha256:sha256`, and `request_count:uint`. `genesis-attest` accepts exactly `schema_version:uint=1`, `operation:genesis-attest-literal`, `event_id:id128`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `request_commit:git_oid`, `requests_manifest_locator:locator`, `requests_manifest_sha256:sha256`, `registry_locator:locator`, `registry_sha256:sha256`, and `authority_provider_id:id128`; it returns exactly `schema_version:uint=1`, `operation:genesis-attest-literal`, `event_id:id128`, `partial_locator:relative_path`, and `partial_sha256:sha256`. `genesis-bundle` accepts exactly `schema_version:uint=1`, `operation:genesis-bundle-literal`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `request_commit:git_oid`, `requests_manifest_locator:locator`, `requests_manifest_sha256:sha256`, and `partials[]`, whose records have exactly `event_id:id128`, `partial_locator:relative_path`, and `partial_sha256:sha256`; it returns exactly `schema_version:uint=1`, `operation:genesis-bundle-literal`, `partials_manifest_locator:relative_path`, `partials_manifest_sha256:sha256`, `bundle_locator:relative_path`, `bundle_sha256:sha256`, and `event_count:uint`. Only these four operations are strict aliases of the corresponding migration plan/prepare/attest/bundle contracts and produce byte-identical artifacts; `genesis-trust` and `genesis-input` are distinct reviewed bootstrap contracts.

`resolve-receipt` accepts exactly `schema_version:uint=1`, `operation:resolve-receipt-literal`, `provider_id:id128`, `provider_commit_oid:git_oid`, `append_result_locator:locator`, and `append_result_sha256:sha256`; it returns exactly `schema_version:uint=1`, `operation:resolve-receipt-literal`, `provider_id:id128`, `provider_commit_oid:git_oid`, `append_result_sha256:sha256`, `receipt_sha256:sha256`, `signature_sha256:sha256`, `authorization_result_sha256:sha256`, `provider_sequence:uint`, and `verified:true-literal`. It is read-only and refetches all bytes from protected ancestry. The remaining seven command schemas are the exact authorize, blob publication/resolution, generic lookup, append, receipt lookup, and registry-CAS schemas below. Unknown keys, wrong operation literals, unresolved locators, and digest mismatches fail without provider mutation.

Event authority is closed: `lane_classified`, `requirement_declared`, `production_ac_declared`, `parent_intent_bound`, `prework_contract_frozen`, `implementation_authorized`, `implementation_bound`, `legacy_claim_imported`, and `event_superseded` require `governance`; `source_manifest_bound`, `remark_captured`, and `child_manifest_bound` require `source-provider`; `atomicity_reviewed` requires `atomicity-reviewer`; `evolution_requested`, `evolution_blocked`, `evolution_declined`, `evolution_applied`, and `evolution_disposition` require `evolution-owner`; `evolution_approved` and `evolution_rejected` require `governance`; `evolution_reviewed` requires `evolution-reviewer`; `red_observed` and `green_observed` require `evidence-runner`; `production_deployment_observed` requires `deployment-provider`; `visual_observation` requires `visual-provider`; `customer_disposition` requires `customer-authority`; `enabling_disposition` requires `project-verifier`; and `head_reconciled` requires `reconciliation-authority`.

Receipt authority is closed: `event-attestation` requires the exact signer key/role/namespace declared by its subject event and permitted by the event-authority table; `anchor` requires `anchor-authority`; `source-manifest` requires `source-provider`; `atomicity-review` requires `atomicity-reviewer`; `spec-review` requires `spec-reviewer`; `evolution-approval` and `evolution-rejection` require `governance`; `evolution-decline` requires `evolution-owner`; `evolution-review` requires `evolution-reviewer`; `quality-review` requires `quality-reviewer`; `runner-red` and `runner-green` require `evidence-runner`; `project-verification` requires `project-verifier`; `customer-disposition` requires `customer-authority`; `deployment` requires `deployment-provider`; `visual` requires `visual-provider`; `authorization` and `provider-registration` require `governance`; `reconciliation` requires `reconciliation-authority`; `migration-recovery` requires `anchor-authority`; and `release-tag` requires `release-authority`. Catalog receipt profiles permit `event-attestation` for exactly the roles present in the event-authority table, and each signer `receipt_types[]`/namespace/scope remains role-constrained; no role or provider may widen the table.

Every signed semantic receipt is complete before provider publication. The provider's independently authenticated append result assigns its publication sequence and binds the exact receipt/signature digests; no semantic receipt template contains a sequence placeholder.

Catalogs, consumer registries, revocations, receipts, and their public keys remain retrievable as immutable blobs on the monotonic authority; a valid signature on a replayed older registry is insufficient. Normal rotation requires old-and-new continuity. Compromise recovery uses the previous catalog's independent offline recovery quorum to bind the deterministic successor preimage and every compromised/replacement key; it neither requires nor accepts a signature from a compromised governance, registry-owner, or anchor key. A separate quorum-signed recovery-CAS authorization binds the final successor request/digest after the bundle and pending-key proofs exist. Registry CAS uses the predecessor registry's fixed stable result-signer identity: if that key is in the recovery delta, the quorum-authorized recovery-prepared or quorum-rebound pending successor key signs the result and exact transition-authorized lookups; otherwise the unchanged active key signs. Thus loss or compromise of another ordinary authority key cannot deadlock revocation. Revoked/expired keys invalidate new publication. A revocation is effective at its precomputable successor `registry_sequence`; the provider rejects any new append whose receipt names an older registry after that successor CAS. Historical receipts remain valid only when their receipt registry revision predates `effective_registry_sequence` and their independently verified append result was accepted before the successor CAS control sequence; timestamps or signature time never establish that. Profile downgrade is forbidden after `canon_epoch`. The verifier resolves the historical signed catalog/registry/revocation sequence named by each event or receipt and rejects authority material merely present in the same untrusted tree.

The first shipped provider profiles are concrete:

- `git-protected-ref-v1` is the ongoing monotonic receipt/head authority. It requires a dedicated remote branch whose current host rules forbid deletion and non-fast-forward updates while permitting append-only fast-forwards, verifies those rules through a pinned host API profile on every append/resolve gate, and accepts a receipt or ledger head only when the exact immutable blobs are reachable from that protected ref. Datarim's concrete registry fixes `refs/heads/datarim/customer-delivery-receipts-v1`, its exact GitHub ruleset ID/digest after creation, and the immutable provider-registration blob; downstream projects choose their own exact protected ref. Each strict stage transition compare-appends through this authority before the next stage. Git credentials come from the existing credential helper; tokens are never command arguments.
- `git-immutable-tag-v1` is the one-shot genesis/release anchor. It requires a protected `refs/tags/v*` namespace whose rules, refetched after tag creation, forbid deletion, non-fast-forward, and update with no bypass. A distinct registry signer role `release-authority` is authorized for namespace `git` solely for the embedded SSH tag signature required by `git verify-tag`, and for namespace `datarim-release-v1` for the separate closed `release-tag` receipt embedded in the annotation. Both validations bind the same public key/fingerprint and immutable authority locator; neither namespace substitutes for the other. The canon's reviewed release tag anchors the final genesis tuple but cannot serve subsequent append transitions.
- `caller-sign-v1` is the independently registered caller-key process. Its sole mutation is `caller-sign`; it accepts the closed bootstrap wire above, checks the exact subject bytes against the namespace-specific catalog schema, and returns one caller-proof envelope from the caller record's key. Event/recovery authority requests use `datarim-authority-request-v1`; blob publication requests use the structurally distinct `datarim-provider-blob-request-v1`. Each namespace has its own catalog signature profile and subject-schema digest. A caller record explicitly permits only the needed namespaces and operations; sharing a key does not permit cross-schema substitution.
- `git-ssh-v1` authenticates receipts with `ssh-keygen -Y sign/verify`, one fixed signer/role/provider/policy/caller subject per reciprocal registry caller/signer pair, and a project-registry allowed-signers file pinned by digest. The immutable authorization request has exactly `schema_version:uint=1`, `operation:authorize-and-sign-literal`, `request_id:sha256`, `provider_id:id128`, `caller_subject:id128`, `registry_sequence:uint`, `registry_sha256:sha256`, `context_kind` (`migration-event`, `migration-recovery`, or `ordinary-receipt`), `context_locator:locator`, `context_sha256:sha256`, `receipt_payload_sha256:sha256`, and `receipt_payload_b64:text`; `request_id` hashes the canonical request with `request_id` omitted. The caller signs those exact bytes under `datarim-authority-request-v1` with the active caller key fixed by the unique `(authority_provider_id, caller_subject)` registry record. Its registered argv is exactly `[executable, "authorize-and-sign", "--request", "-"]`; stdin is a pointer envelope with exactly `request_locator:locator`, `request_sha256:sha256`, `caller_signature_locator:locator`, and `caller_signature_sha256:sha256`. The provider refetches/hashes both blobs, verifies the caller signature/key/subject/namespace/operation scope and validity, verifies the invoked `caller_provider_id`, then follows `caller.signer_id` to the one reciprocal active signer and policy, refetches/hashes the immutable context, and independently proves that the receipt type/subject/event/requirement/plan membership and authority decision satisfy that policy. A reviewer policy requires the matching independently produced review verdict; governance, customer, source, recovery, and release policies likewise require their class-specific authority record. The caller cannot select signer, key, role, receipt namespace, or policy, and failure returns no receipt signature.

  The wire success result has exactly `authorization` and `decision_signature`. `authorization` has exactly `schema_version:uint=1`, `request_id:sha256`, `request_locator:locator`, `request_sha256:sha256`, `caller_signature_locator:locator`, `caller_signature_sha256:sha256`, `caller_subject:id128`, `provider_id:id128`, `registry_sequence:uint`, `registry_sha256:sha256`, `context_kind:id128`, `context_locator:locator`, `context_sha256:sha256`, `receipt_payload_sha256:sha256`, `authorization_policy_sha256:sha256`, `algorithm:ssh-ed25519-literal`, `namespace:id128`, `signer_id:id128`, `key_id:id128`, `principal:id128`, and `receipt_sshsig_b64:text`; every field must equal the refetched request, caller proof, and fixed registry records, and the receipt signature covers the canonical receipt payload in that receipt's catalog namespace. `decision_signature` is the exact detached signature envelope over canonical `authorization` bytes under the additional fixed namespace `datarim-authority-decision-v1`; its signer/key/principal must equal `authorization`, and that decision namespace must also be present in the signer's immutable registry record. This second signature cryptographically binds the complete replayable caller/context/policy decision. Private keys and authority decisions stay in the distinct provider process. The migration process can verify/aggregate returned bytes but cannot hold a role key or obtain a signature for a payload outside the fixed policy.
- source, customer, and child providers use the same constrained adapter envelope and an authenticated provider-specific receipt. A project without a passing trust/authority probe cannot arm strict mode.

After plan creation and before apply, caller preparation and authority attestation are separate immutable stages. The closed attestation bundle has exactly `schema_version:uint=1`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `request_commit:git_oid`, `requests_manifest_locator:locator`, `requests_manifest_sha256:sha256`, `registry_sha256:sha256`, `event_attestations[]`, and `bundle_sha256:sha256`; each item, in plan event order, has exactly `event_id:id128`, `event_index:uint`, `signer_id:id128`, `provider_id:id128`, `request_locator:locator`, `request_sha256:sha256`, `caller_signature_locator:locator`, `caller_signature_sha256:sha256`, `receipt_locator:relative_path`, `receipt_sha256:sha256`, `signature_locator:relative_path`, `signature_sha256:sha256`, `authorization_result_locator:relative_path`, `authorization_result_sha256:sha256`, `request`, `caller_signature`, `receipt_payload`, `signature`, and `authorization_result`. Request/caller locators must resolve from `request_commit`; receipt/result locators are deterministic descendants of `receipt_output/`. Every inline object must hash to its locator digest, the request context must equal `plan_locator`/digest at `plan_commit`, and clean Git-object resolution must return the exact plan/request bytes. `bundle_sha256` hashes the canonical bundle with that field omitted. The item set must equal the plan event set; the receipt signature must equal `authorization_result.authorization.receipt_sshsig_b64`, and the caller, receipt, and decision signatures must all verify against the fixed caller/provider/policy. Apply accepts this complete bundle and never invokes an event caller or event-authority signer; only the separately planned recovery pair may run during apply. The anchor provider is separately probed before apply and may authorize only state-machine-valid `migration-recovery` receipts under its fixed recovery policy; its unavailability blocks without changing provider state.

The plan must perform one hard outer eight-minute feasibility probe of executable availability, signer verification, remote rules, credential scope, and an append/resolve dry run before implementation. All provider and evidence-runner deadlines are expressed as integer minutes, never ambiguous seconds. The outer supervisor records a monotonic absolute deadline and refuses to start any child unless `deadline_minutes * 60 + 5` seconds of SIGTERM grace plus a fixed 10-second evidence-fsync reserve fit before that deadline; after every child it fsyncs command evidence before another launch. The eight-minute watchdog is therefore only a final fault boundary, never the normal mechanism that truncates child cleanup or evidence. A dedicated negative-control command has a one-minute child deadline within that longer probe and passes only when the supervisor sends SIGTERM to the child process group and records normalized process exit `143` (`128 + SIGTERM`), `raw_wait_status`, `termination_signal:15`, `timed_out:true`, and no surviving descendant; this expected negative result does not make the overall probe fail. An unexpected deadline on any ordinary probe command is a failure and is never collapsed into a generic error. A child that does not terminate during the fixed five-second cleanup grace is force-killed, with `forced_kill:true` and raw wait status retained, while the contracted deadline result remains exit `143`. Documentation is not proof of provider behavior.

Adapters are data providers, never shell fragments or state authorities. Registration is accepted only from the project-governance location and authority pinned by the framework consumer registry. It uses argv arrays with an allowlisted executable, pinned executable digest/version, fixed project-relative working directory, explicit environment allowlist, an integer-minute deadline, stdout-size cap, and authenticated/schema-validated JSON results. The validator invokes without a shell, rejects path escapes and symlinks, applies the SIGTERM/exit-143 deadline contract above, and independently recomputes coverage. Adapters cannot emit derived state or passing assertions. A customer-authority adapter may resolve an existing signed acceptance record but cannot create one; the core verifies its trust proof. The separate head-anchor adapter exposes append/resolve operations backed by a monotonic provider and rejects rollback or divergent heads.

Timeout fields have closed cross-field semantics. For a normally exited process, `timed_out=false`, `termination_signal=null`, `forced_kill=false`, `result_exit_code` equals the actual 0–255 exit, and `raw_wait_status` is the POSIX wait status. For deadline termination during SIGTERM grace, `timed_out=true`, `termination_signal=15`, `forced_kill=false`, `result_exit_code=143`, and raw wait status reports SIGTERM. For a SIGTERM-resistant process group, `timed_out=true`, `termination_signal=15`, `forced_kill=true`, `result_exit_code=143`, and raw wait status reports the eventual SIGKILL cleanup. Exit `143` with `timed_out=false`, a non-null signal on a normal exit, `forced_kill=true` without timeout, any other timeout exit, or any surviving child/grandchild is invalid.

Every protected-ref mutation (`publish-blob`, `append-receipt`, or `registry-cas`) uses one common control chain. The control record has exactly `schema_version:uint=1`, `provider_id:id128`, `operation` (`publish-blob`, `append-receipt`, or `registry-cas`), `idempotency_key:sha256`, `request_sha256:sha256`, `request_authorization_sha256:sha256|null`, `previous_provider_head:sha256`, `provider_sequence:uint`, and `object_refs[]`; each object ref has exactly `kind`, `locator:locator`, and `sha256:sha256`; kind is one of `caller-proof`, `blob`, `receipt`, `receipt-signature`, `authorization-result`, `cas-request-authorization`, `replacement-registry`, `replacement-registry-authorization`, `key-activation-request`, `key-activation-authority`, `key-activation-prepare-result`, `key-activation-prepare-proof`, or `anchor-receipt`, is canonical-byte sorted by `(kind, locator, sha256)`, and is duplicate-free. Blob publication's object set is exactly caller proof plus every decoded blob; receipt append's is exactly receipt, receipt signature, and authorization result; registry CAS's is exactly request authorization, replacement registry, replacement authorization, every ordered activation request/authority/prepare-result/prepare-proof tuple named by a non-empty key delta, and a non-null anchor receipt when required. `request_authorization_sha256` equals the blob caller proof, append authorization result, or mode-specific CAS authorization respectively. `result_provider_head` is lowercase SHA-256 of the exact canonical control record. The first mutation uses sequence `0` and 64 zeroes as predecessor; every later mutation increments once and names the preceding control hash. Provider-owned paths are exactly `requests/<operation>/<idempotency_key>.json`, `authorizations/<operation>/<idempotency_key>.json` when non-null, `controls/<20-digit-zero-padded-decimal-sequence>.json`, `indexes/<operation>/<idempotency_key>.json`, `results/<operation>/<idempotency_key>.json`, and `results/<operation>/<idempotency_key>.json.sig`, plus content-addressed `blobs/<sha256>`. Consumer plans may name an external artifact role, but that role is never a provider path: it resolves only through the authenticated operation result/control to one of these exact paths, and verification rejects any semantic pseudo-path such as `receipt-authority/<task>/<name>`. The index is byte-identical to the control record and exists only to locate it; neither record contains a Git commit OID or result digest. One prepared Git commit contains the complete exact operation set, control/index, result, and result signature; one protected-ref CAS exposes all or none. Every result carries the control's predecessor/head/sequence and is signed by the provider record's one fixed `result_signer_id`. Thus Git traversal, control-head recomputation, and signer selection are identical across all three mutating operations.

Read-only `lookup-provider-operation` accepts exactly `schema_version:uint=1`, `operation:lookup-provider-operation-literal`, `provider_id:id128`, `target_operation` (`publish-blob`, `append-receipt`, or `registry-cas`), `idempotency_key:sha256`, and `transition_authorization`. `transition_authorization` is null normally or, only for `registry-cas`, has exactly `registry_cas_request_locator:locator`, `registry_cas_request_sha256:sha256`, `request_authorization_locator:locator`, `request_authorization_sha256:sha256`, `activation_request_locator:locator`, `activation_request_sha256:sha256`, `authority_bundle_locator:locator`, `authority_bundle_sha256:sha256`, `prepare_result_locator:locator`, `prepare_result_sha256:sha256`, `prepare_proof_locator:locator`, and `prepare_proof_sha256:sha256`; every blob is refetched, the CAS request idempotency key must equal the lookup target, and the activation tuple may name any exact caller, signer, or governance transition retained by that request. This object authorizes the ancestry query; it authorizes lookup-signing key substitution only when the named transition is this provider's fixed stable result signer. It returns exactly `lookup` and `signature`; lookup has exactly `schema_version:uint=1`, `provider_id:id128`, `target_operation`, `idempotency_key:sha256`, `status` (`absent` or `present`), `observed_provider_head:sha256`, `observed_provider_sequence:uint|null`, `observed_provider_commit_oid:git_oid|null`, `observed_registry_sequence:uint|null`, `observed_registry_locator:locator|null`, `observed_registry_sha256:sha256|null`, `observed_registry_cas_idempotency_key:sha256|null`, `observed_registry_result_locator:locator|null`, `observed_registry_result_sha256:sha256|null`, `observed_registry_result_signature_locator:locator|null`, `observed_registry_result_signature_sha256:sha256|null`, `observed_registry_provider_commit_oid:git_oid|null`, `activation_acceptances[]`, `result_provider_commit_oid:git_oid|null`, `result_locator:locator|null`, `result_sha256:sha256|null`, `result_signature_locator:locator|null`, and `result_signature_sha256:sha256|null`. For a provider with no control commits, observed provider sequence/commit and all target-result fields are null and observed provider head is zero; an initialized registry-CAS provider still returns its non-null initialized registry sequence/locator/digest while its six registry-CAS provenance fields remain null. For a registry-CAS provider, the observed registry sequence/locator/digest always name its initialized or latest accepted authoritative registry tuple; for a provider profile without registry state, all three are null. The six observed registry-CAS provenance fields are all null for the initialized tuple and non-registry providers, or all non-null after a registry CAS and identify/refetch/verify the unique control/result/signature/introducing commit that installed the current tuple. For absent on a non-empty provider, observed provider sequence/commit name the current control head while every result field is null. Present requires both observed provider fields and all result fields non-null; `result_provider_commit_oid` identifies the introducing commit. Present traverses the protected control index, returns the Git commit that introduced the byte-identical result/signature, and verifies every object reference.  With null transition authorization, `activation_acceptances[]` is empty. With non-null transition authorization, the provider traverses the complete protected control ancestry and the array contains zero or one record for the referenced activation; a record has exactly `activation_nonce:id128`, `slot_lineage_sha256:sha256`, `accepted_activation_action` (`prepare` or `rebind`), `accepted_activation_request_locator:locator`, `accepted_activation_request_sha256:sha256`, `accepted_authority_bundle_locator:locator`, `accepted_authority_bundle_sha256:sha256`, `accepted_activation_result_locator:locator`, `accepted_activation_result_sha256:sha256`, `accepted_activation_proof_locator:locator`, `accepted_activation_proof_sha256:sha256`, `registry_cas_idempotency_key:sha256`, `registry_cas_result_locator:locator`, `registry_cas_result_sha256:sha256`, `registry_cas_result_signature_locator:locator`, `registry_cas_result_signature_sha256:sha256`, and `provider_commit_oid:git_oid`. The query derives the closed slot-lineage record from its activation request/result/proof. Complete ancestry matches any accepted prepare or rebind with that exact lineage; a rebind is an alias only when its `original_prepare_request_sha256`, nonce, identity/key/generation fields, and both possession proofs verify the journal-authorized link. The array therefore returns the actual winning tuple even when the query names the losing alias and regardless of later registry transitions; more than one accepted CAS for one lineage is integrity failure. Supersession requires this array empty; one match always resumes finalize from that historical introducing CAS. The provider's fixed stable result-signer identity signs lookup under `datarim-provider-lookup-result-v1`, with its active key for null transition authorization and for any queried activation that does not rotate that signer. When a normal continuity or recovery CAS rotates that signer, the exact prepared pending successor key may sign an `absent` or `present` lookup only with the non-null transition authorization above. Verification binds the continuity/recovery authority, prepare result/proof, signed CAS request, observed protected head, and, when present, accepted control and CAS result; the key has no authority over another idempotency key or operation. This operation never invokes a caller and recovers an accepted response after caller loss or revocation.

Immutable context publication is a provider-native, non-recursive operation. `publish-blob` receives exactly `request` and `caller_signature`. The request has exactly `schema_version:uint=1`, `operation:publish-blob-literal`, `provider_id:id128`, `caller_subject:id128`, `registry_sequence:uint`, `registry_sha256:sha256`, `purpose` (`migration-recovery-context`, `authority-context`, `registry-context`, or `evidence-context`), `subject_id:sha256`, `idempotency_key:sha256`, and `blobs[]`; each blob has exactly `media_type:id128`, `blob_sha256:sha256`, `byte_length:uint`, and `blob_b64:text`. Blob records are canonical-byte sorted by digest and duplicate-free; Base64 is padded RFC 4648 and decodes to exactly the declared bytes/digest/length. `idempotency_key` is SHA-256 of canonical JSON containing exactly `operation`, `provider_id`, `caller_subject`, `registry_sequence`, `registry_sha256`, `purpose`, and `subject_id`; blob and caller-proof bytes are deliberately excluded but remain bound by `request_sha256`, and any changed signed request under the same key fails. For `migration-recovery-context`, `subject_id` is SHA-256 of canonical JSON containing exactly plan digest, transition index, phase, and manifest-preimage digest, so it is derivable before and after caller loss; every other purpose requires a governance-authorized stable semantic subject ID fixed before caller signing. `caller_signature` is the detached envelope over exact request bytes under `datarim-provider-blob-request-v1`; the unique active caller record must bind the independently invoked caller provider to this authority provider and permit both that namespace and `publish-blob`. The provider applies the common one-CAS control contract above. The result has exactly `schema_version:uint=1`, `operation:publish-blob-literal`, `provider_id:id128`, `idempotency_key:sha256`, `request_sha256:sha256`, `request_authorization_sha256:sha256`, `previous_provider_head:sha256`, `result_provider_head:sha256`, `provider_sequence:uint`, `blobs[]`, and `provider_ref:text`; each ordered result blob has exactly `blob_locator:locator`, `blob_sha256:sha256`, and `byte_length:uint` and equals the request set. The wire response has exactly `result`, `signature`, and `provider_commit_oid`; the provider's fixed result signer signs canonical result bytes under `datarim-provider-blob-result-v1`, while the commit OID remains outside signed bytes and is verified from protected ancestry or recovered by generic lookup. `resolve-blob` accepts exactly `schema_version:uint=1`, `operation:resolve-blob-literal`, `provider_id:id128`, `blob_locator:locator`, and `blob_sha256:sha256`; it returns exactly `resolution` and `signature`, where resolution has exactly `schema_version:uint=1`, `operation:resolve-blob-literal`, `provider_id:id128`, `provider_commit_oid:git_oid`, `blob_locator:locator`, `blob_sha256:sha256`, `byte_length:uint`, `provider_sequence:uint`, and `verified:true-literal`, signed by the fixed result signer under `datarim-provider-blob-resolve-v1`. Resolution traverses protected ancestry and chooses the lowest provider sequence that introduced those exact content-addressed bytes; it is read-only. Content locators are deterministically derived from provider identity and digest, so a caller can sign an authority request naming a not-yet-published locator; the authority may use it only after blob publication is recovered/refetched. An identical signed publish wire returns the byte-identical existing response; changed request or signature bytes under the same idempotency key fail; a pre-CAS crash consumes no sequence; and a post-CAS response loss is recovered with `lookup-provider-operation` without invoking or re-signing as the caller.

Receipt publication is one provider-native atomic operation with no reservation. Its request has exactly `schema_version:uint=1`, `operation:append-receipt-literal`, `provider_id:id128`, `idempotency_key:sha256`, `receipt_locator:locator`, `receipt_sha256:sha256`, `signature_locator:locator`, `signature_sha256:sha256`, `authorization_result_locator:locator`, and `authorization_result_sha256:sha256`; its idempotency key must equal the common receipt idempotency key. The provider refetches/hashes all three blobs, proves the receipt signature is the exact signature embedded in the authenticated authorization result, replays that result's immutable request/caller/context/policy proofs, verifies the complete semantic receipt role/namespace/scope and registry boundary, rejects revoked/expired authority at publication, then serializes one append under its own lock/CAS. Concurrent callers require no caller-supplied head and cannot reserve or strand an ordinal.

The append result has exactly `schema_version:uint=1`, `provider_id:id128`, `idempotency_key:sha256`, `request_sha256:sha256`, `request_authorization_sha256:sha256`, `previous_provider_head:sha256`, `result_provider_head:sha256`, `provider_sequence:uint`, `receipt_id:id128`, `receipt_type:id128`, `registry_sequence:uint`, `registry_sha256:sha256`, `receipt_locator:locator`, `receipt_sha256:sha256`, `signature_locator:locator`, `signature_sha256:sha256`, `authorization_result_locator:locator`, `authorization_result_sha256:sha256`, and `provider_ref:text`; `request_authorization_sha256` equals `authorization_result_sha256`. The append wire response has exactly `result`, `signature`, and `provider_commit_oid`; the provider record's fixed result signer signs canonical result bytes under namespace `datarim-provider-append-result-v1`, while `provider_commit_oid` is verified from protected ancestry and remains outside signed bytes to avoid a Git fixed point. The provider applies the common one-CAS control, path, head, and sequence contract above. A clean resolver fetches the introducing commit, recomputes every request/object/control/result/signature byte, and verifies the fixed result signer; no stored object names its own commit OID, and there is no content-only acknowledgement window.

The legacy CLI spelling `lookup-receipt` is only an argv alias. It accepts the exact `lookup-provider-operation` request with `operation:lookup-provider-operation` and `target_operation:append-receipt`, and returns the byte-identical generic lookup payload/signature under the one `datarim-provider-lookup-result-v1` schema. It defines no second request, result, namespace, or transformation. Identical append requests return the byte-identical existing append response after later mutations; reuse of an idempotency key with any changed request or authorization byte fails. A pre-CAS crash consumes no sequence; a post-CAS response loss is recovered by generic lookup. A semantic signer may be unavailable or revoked before append without consuming provider state. Thus no reservation, cancellation, abort receipt, sequence gap, compound acknowledgement window, or post-hoc signer substitution exists.

### 10. Writer durability and authority transitions

The writer serializes per-task appends with an advisory lock that carries owner/process/start metadata and a bounded stale-lock recovery rule. It validates the current anchored head, writes and fsyncs a complete temporary file, atomically renames, fsyncs the containing directory, publishes the complete immutable blob, then requests a compare-and-append head receipt. Duplicate retries are idempotent by event ID and payload digest. Concurrent/divergent predecessors fail without overwriting either branch. Disk-full, short write, signal/process death, publication/anchor timeout, and failure between rename/publication/anchor leave an explicit recoverable `unanchored` state; no stage gate passes until reconciliation proves the exact published blob and anchors it.

Supersession is deterministic. `event_superseded` may target only `lane_classified`, `source_manifest_bound`, `requirement_declared`, `production_ac_declared`, `parent_intent_bound`, `child_manifest_bound`, or `prework_contract_frozen`; it must occur before any valid `implementation_authorized`, use governance authority, point to an already-present same-typed replacement event with a greater event index, and list the exact downstream review/freeze invalidation set. At the supersession predecessor, both target and replacement must be active; a target may be superseded at most once, a replacement may be named by at most one supersession, and the directed replacement graph must be disjoint acyclic chains with exactly one active tip each. A branch, merge, cycle, inactive/already-superseded replacement, or replacement back to an ancestor fails. Every other target type or actor fails. Remark captures, atomicity reviews, all evolution history/dispositions, authorization, implementation/evidence/deploy/visual observations, dispositions, reconciliations, legacy claims, and external receipts are never semantically removed. When a source manifest is superseded, its remark captures remain preserved historical events but become non-covering; the replacement active manifest must have its own exact captures and a new atomicity review/freeze, and stale captures cannot satisfy or appear as extras in the active denominator. A declaration supersession invalidates atomicity review/freeze and all downstream events.

Implementation/evidence/deploy/visual observations are countermanded only by a newer valid observation with the same typed subject identity and scoped runner/provider signer; the highest `event_index` on one reconciled anchored chain wins. Customer disposition is resolved by customer-provider sequence, never ledger order, and returned/rejected reopens. Two events with the same predecessor are a divergence and neither wins. `head_reconciled` is appended only to the authority-chosen branch, contains canonical-sorted `conflict_ref` pairs for every conflicting event/head, requires equal shared predecessor and at least two pairs, chooses exactly one paired event ID, and carries a `reconciliation` receipt signed by `reconciliation-authority`; the external authority preserves every losing blob/head. A reconciliation missing a branch, inventing an association, or choosing a non-member fails closed.

### 11. Genesis bootstrap

A framework cannot use an unimplemented runner/anchor to prove its own creation. The sole bootstrap is explicit, narrow, independently authorized, and non-passing:

1. An exact PRD/plan commit exists first and receives two independent subordinate `SPEC READY: YES` verdicts; no canonical receipt is claimed yet.
2. The pre-existing pinned governance root directly signs the closed bootstrap-authorization object described above. A signed bootstrap-RED commit then records the exact missing-executable assertion and tests before source.
3. One bootstrap-source commit adds only the authorized standard-library executable and reviewed bootstrap wire-schema paths. A separate bootstrap-review commit preserves an exact-head subordinate quality report and governance-signed review record. Failure requires a new source/review commit; history is not rewritten.
4. The reviewed bootstrap executable creates and verifies the signed bootstrap registration. Governance freezes the exact closed `genesis-trust` request alone in a signed `trust-request` commit, then its direct-child governance-signed `trust-invocation` commit binds that exact request commit and allowed apply/lookup actions. Only through that invocation does the reviewed operation provision keys at private handles, create/read back the permanent protected ref/ruleset, and emit the closed signed result, catalog, caller/signer/provider records, Datarim consumer registry in `genesis` mode with null epoch, allowed-signers view, provider-registration manifest, and detached signatures in `trust-provision`. Bootstrap authorization/RED/source/review are retained but do not count as canonical task evidence until the later runner rechecks them.
5. A subordinate spec reviewer reviews the unchanged exact PRD/plan pair using the now-provisioned reviewer identity. The caller-signed request and semantic receipt template are committed first as `spec-review-request`; only then does the provider resolve those immutable bytes, sign under policy, and atomically append/refetch the receipt. No future-registry placeholder or mutable request locator is permitted.
6. A signed contract-base commit creates the closed requirement-scoped contract manifest before any freeze event and binds every active requirement to its parent/evolution edges plus all six capability classes.
7. A separate genesis-freeze commit records the spec-review receipt/signature, private-source commitments, atomic requirement mapping/review, the pre-existing contract manifest, target base, provider feasibility, and selected profiles. It may not pin a skill or general implementation tool that does not yet exist.
8. A marker commit records the clean general-implementation base. The caller-signed request and semantic authorization receipt over the known freeze/marker commits are then frozen in a separate `authorization-request` commit before governance signs or publishes that receipt.
9. The source provider emits the exact exhausted manifest/signature and revision/signature; governance co-freezes those four blobs with the closed `genesis-input` request/signature in `attestation-input-request`. Its direct-child governance-signed `attestation-input-authorization` commit binds that exact request commit and allowed apply/lookup actions. The reviewed bootstrap then deterministically creates the signed genesis-input result plus exact migration input/source snapshot/plan in `attestation-plan`. Fixed caller providers create the complete request/caller-proof manifest in separate signed `attestation-requests`; every fixed-policy authority process resolves both commits and signs only its mapped planned event, and `genesis-bundle` creates the partial manifest/bundle preserved in `authorization-event`. Planned event receipts are not appended yet, so transition-zero remains legal; only the separate authorization receipt was published. No request, caller proof, or event attestation is reconstructed after general implementation starts.
10. General repository TDD begins only in a descendant of the authorization-event commit and later applies the frozen plan/bundle. Apply first durably creates transition-zero, then appends/refetches planned event receipts through the one-commit provider protocol. Raw Bats output is development evidence, not canonical runner attestation.
11. Once the runner exists, present-day controlled mutations produce canonical signed RED, restored exact heads produce signed GREEN, and the runner rechecks the bootstrap chain. An independent quality reviewer then signs an external exact-head receipt and compare-appends it to the permanent protected receipt authority; the implementation head does not change after review.
12. The bootstrap remains `genesis_unanchored` and cannot report canonical completion until that exact reviewed head is merged, the signed release tag annotation binds the full merged commit/tree and ledger blob/event head/count, the current immutable-tag ruleset still forbids deletion/update/non-fast-forward with no bypass, and an independent verifier refetches the ruleset, tag/receipt, and ledger bytes through `git-immutable-tag-v1`. Governance then constructs consumer-registry sequence `n+1` whose predecessor is the verified genesis registry and whose non-null `canon_epoch` binds release version, tag object, peeled commit, ledger blob/head, and anchor receipt; it signs, publishes through the exact `registry-cas` request, refetches, and verifies the authenticated result. Only that successor makes the verified tag receipt canonical and permits completion. It requires no recursive in-ledger or next-release Git commit. All later tasks use the normal strict sequence with the permanent `git-protected-ref-v1` receipt branch.

This exception applies only to the implementation of the canon's own missing enforcement machinery. It never converts historical output into pre-implementation RED and never applies to Talomnia or later consumers.

## Requirements

#### D-REQ-01: Append-only atomic source mapping

Every authoritative source-manifest item is exhausted and has its own hash-chained capture event, stable source digest, and at least one atomic requirement ID; every requirement has at least one remark ID. Duplicate IDs, incomplete/paginated manifests, broken predecessor/event hashes, dangling mappings, empty verbatim text, source-digest mismatch, omitted source items, or history rewriting block all later phases.

#### D-REQ-02: Visitor-visible AC or enabling parent

Every task classifies its delivery lane and every armed requirement declares exactly one class. A customer-visible requirement needs one atomic typed observable production outcome, target surface, and immutable blueprint. An enabling requirement needs a valid customer-visible parent from the authoritative manifest and cannot contribute delivered coverage to that parent. `none` is rejected when source intent names a customer or visitor outcome.

#### D-REQ-03: Immutable pre-work capability bindings

Role, skill, blueprint, constraint, policy, and success criterion are all pinned with immutable revision and digest before implementation authorization. Any `Unbound`, `—`, mutable reference, missing class, absent freeze blob/authorization marker, target repository mismatch, dirty authorization base, or non-descendant implementation commit is a hard failure.

#### D-REQ-04: Review-to-evolution

Customer-review requirements carry a pre-work evolution disposition and pinned artifacts. The pre-work gate goes RED when the disposition is absent or post-hoc and GREEN only when the requirement is covered before implementation. An approved request alone may apply changes; a rejected request remains blocked, cannot apply or freeze, and may resume only through an authenticated terminal decline or a distinct new request ID.

#### D-REQ-05: RED/GREEN evidence integrity

Each requirement carries distinct load-bearing RED and GREEN records. Evidence records include command/test identity, exit code, artifact digest, timestamp, and retained path. Identical/vacuous evidence, missing mutation assertion, or missing retained artifact blocks completion.

#### D-REQ-06: Visitor-visible live delivery

A visitor-visible requirement is incomplete without exact deployment provenance, a semantic post-deploy live probe, all eight non-aliased screenshot cells, and customer-authenticated acceptance or withdrawal. Returned, rejected, or deferred work is not complete. Static or local artifacts cannot satisfy this requirement.

#### D-REQ-07: Derived task and epic state

Coverage is computed from authoritative source and child manifests, mappings, requirements, evidence, and customer dispositions. The command emits machine-readable JSON and human-readable summaries with exact missing IDs. No ledger-authored denominator or stored completion field is accepted as authority.

#### D-REQ-08: Phase-specific command gates

Every task-creating or task-advancing entry point classifies the lane and invokes the validator at the appropriate phase. The lifecycle, quick, content, replay, status, and orchestration commands named above fail closed on armed invalid contracts. Wiring tests discover command capabilities and reject any new bypass rather than merely grepping a fixed nine-command list.

#### D-REQ-09: Compatibility and migration

New L1-L4 tasks classify their lane after the exact framework release/version/commit recorded as `canon_epoch`. Armed lanes are hard across all complexity levels. The consumer registry, outside the task ledger, records a monotonic adoption state that cannot revert from strict to legacy or `none`. Pre-epoch tasks are historical unless explicitly registered for strict adoption. A migration helper imports genuine source bytes as capture events, labels retroactive seeds as `legacy_unverified_source`, and imports existing implementation/tests/screenshots/deploy text only as `legacy_claim_imported` events that do not satisfy coverage. It never invents remarks, bindings, RED/GREEN, live proof, or dispositions. Dual-read compatibility lasts exactly one release and has a named removal version.

Migration is byte-deterministic and split into plan/prepare/attest/bundle/apply. Planning is `migrate --plan --mode genesis|legacy-v0 --task <id> --project <id> --input <migration-input-json> --source-snapshot <signed-manifest-json> --registry <signed-consumer-json> --authorization-request-commit <full-sha-or-null> --canon-epoch <version@full-sha-or-null> --compatibility-release <version-or-null> --removal-version <version-or-null> --recorded-at <UTC-seconds> --receipt-output <relative-dir> --output <migration-plan-json>`. For genesis, `authorization_request_commit` is exactly the pre-existing signed commit that contains the caller-signed authorization request and receipt template; legacy mode requires null. After the plan is committed, caller preparation is `migrate --prepare --plan <committed-plan-json> --plan-commit <full-sha> --plan-locator <git-object-locator> --plan-sha256 <lowercase-hex> --registry <signed-consumer-json> --output <requests-manifest-json>`; for each plan event it resolves and invokes that event's registry-fixed caller provider, then writes every request/caller proof and their manifest before authority use. One isolated authority invocation per event is `migrate --attest --event-id <id> --plan <committed-plan-json> --plan-commit <full-sha> --plan-locator <git-object-locator> --requests <committed-requests-manifest-json> --request-commit <full-sha> --registry <signed-consumer-json> --authority-provider <registered-authority-provider-id> --output <partial-attestation-json>`; the provider argument must equal that event's frozen binding, so a signer used by several events is invoked separately for each event. Deterministic aggregation is `migrate --bundle --plan <committed-plan-json> --plan-commit <full-sha> --plan-locator <git-object-locator> --requests <committed-requests-manifest-json> --request-commit <full-sha> --partial <partial-attestation-json>... --partials-output <partials-manifest-json> --output <attestation-bundle-json>`; repeated partial arguments must cover the plan event set exactly and are canonicalized into event-index order. Apply is `migrate --apply --plan <migration-plan-json> --plan-commit <full-sha> --plan-locator <git-object-locator> --plan-sha256 <lowercase-hex> --requests <committed-requests-manifest-json> --request-commit <full-sha> --input <migration-input-json> --source-snapshot <signed-manifest-json> --registry <signed-consumer-json> --attestation-bundle <attestation-bundle-json> --receipt-output <relative-dir> --output <ledger-jsonl>`. All paths are project-root-confined regular files; a Git-object locator resolves one exact repository/commit/path tuple. Planning and aggregation read no wall clock or network. Preparation, attestation, and recovery use only the providers frozen in the plan/registry; the CLI cannot select a different caller or signer.

Migration schema v1 is closed in `config/customer-delivery-migration-v1.schema.json`. A source snapshot has exactly `schema_version:uint=1`, `provider_id:id128`, `provider_sequence:uint`, `manifest_id:id128`, `manifest_head:sha256`, `items[]`, `item_count:uint`, `pagination_cursor_sha256:sha256`, `ordered_commitment_sha256:sha256`, `exhausted:true`, `anchor_receipt:receipt_ref`, and `signature:receipt_ref`. A source item has exactly the `remark_captured` payload fields other than `authority`, in provider order. A migration input has exactly `schema_version:uint=1`, `mode`, `project_id:id128`, `task_id:id128`, `lane`, `parent_intent` (`parent_intent_bound` payload without authority or null), `source_items[]`, `requirements[]`, `production_acs[]`, `atomicity_review` (payload without authority), `evolution_events[]` (closed event payloads without authority), `prework_contract` (payload without authority), `authorization` (payload without authority or null for legacy), and `legacy_claims[]` (legacy payloads without authority). Requirements and ACs use their exact event payload schemas without authority. Arrays preserve the authenticated provider or declared semantic order; counts/digests must match.

A migration plan has exactly `schema_version:uint=1`, `profile:migration-v1-literal`, `mode`, `project_id:id128`, `task_id:id128`, `input_sha256:sha256`, `source_snapshot_sha256:sha256`, `registry_sha256:sha256`, `registry_sequence:uint`, `authorization_request_commit:git_oid|null`, `canon_epoch` (consumer epoch shape or null), `compatibility_release:text|null`, `removal_version:text|null`, `recorded_at:utc_seconds`, `event_signers[]`, `recovery_caller`, `recovery_signer`, `receipt_output:relative_path`, `provider_id:id128`, `ordered_input_sha256:sha256`, `events[]`, `event_count:uint`, `ledger_byte_length:uint`, `ledger_sha256:sha256`, `ledger_head:sha256`, `coverage_classification:id128`, and `findings[]`. Each signer binding has exactly `signer_id:id128`, `key_id:id128`, `role:authority_role`, `namespace:id128`, `provider_id:id128`, `authorization_policy_sha256:sha256`, `caller_subject:id128`, and `caller_provider_id:id128`; event signers exactly equal distinct mapped event authorities. `recovery_signer` separately equals both the registry's `migration-recovery` authority and the append provider's fixed `result_signer_id`; `recovery_caller` has exactly `caller_subject:id128`, `key_id:id128`, `caller_provider_id:id128`, `authority_provider_id:id128`, and `signer_id:id128`, reciprocally names that recovery signer and its independently registered caller-key process, and is the only caller apply may invoke. Each plan event has exactly `semantic_source_id:id128`, `event` (one complete closed schema-v1 event including `event_hash`), `event_attestation_id:id128`, `event_attestation_signer_id:id128`, and `receipt_template`. Its signer and caller resolve uniquely and equal the event authority pair. The template is the complete common `event-attestation` receipt payload; `issued_at` equals plan `recorded_at`; `receipt_id` is `mig-receipt:` plus the event ID digest; and `idempotency_key` is SHA-256 of canonical JSON containing exactly `profile`, `ordered_input_sha256`, `event_index`, `event_id`, and `event_hash`. Each finding has exactly `code:id128`, `subject_id:id128`, and `message:text`. Canonical event lines exactly match planned ledger byte length/SHA/head. Unknown keys fail.

A requests manifest has exactly `schema_version:uint=1`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `registry_sha256:sha256`, and `requests[]`; each ordered item has exactly `event_id:id128`, `event_index:uint`, `authority_provider_id:id128`, `caller_provider_id:id128`, `caller_subject:id128`, `request_locator:relative_path`, `request_sha256:sha256`, `caller_signature_locator:relative_path`, and `caller_signature_sha256:sha256`. It contains every plan event once in event-index order and both provider IDs must equal its frozen signer binding; after its commit, each locator is interpreted as the exact Git object at that request commit. A partial attestation has exactly `schema_version:uint=1`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `request_commit:git_oid`, `requests_manifest_locator:locator`, `requests_manifest_sha256:sha256`, `registry_sha256:sha256`, `event_id:id128`, `event_index:uint`, `signer_id:id128`, `provider_id:id128`, `request_locator:locator`, `request_sha256:sha256`, `caller_signature_locator:locator`, `caller_signature_sha256:sha256`, `receipt_locator:relative_path`, `receipt_sha256:sha256`, `signature_locator:relative_path`, `signature_sha256:sha256`, `authorization_result_locator:relative_path`, `authorization_result_sha256:sha256`, `request`, `caller_signature`, `receipt_payload`, `signature`, and `authorization_result`; every inline object hashes to its named digest and every locator resolves those bytes. Each `attest` invocation emits exactly the selected event's partial and rejects a provider/event mismatch. The partials manifest has exactly `schema_version:uint=1`, `plan_commit:git_oid`, `plan_locator:locator`, `plan_sha256:sha256`, `request_commit:git_oid`, `requests_manifest_sha256:sha256`, and `partials[]`; each ordered item has exactly `event_id:id128`, `event_index:uint`, `partial_locator:relative_path`, and `partial_sha256:sha256`. `bundle` creates this manifest and the bundle together from the repeated partial inputs; it requires every plan event exactly once in event-index order. Bundle is a deterministic projection of those exact partial bytes, never a re-signing operation.

The immutable plan contains all input SHA-256 values, exact append provider, fixed event/recovery caller and authority-provider bindings and policies, exact epoch/tag/object/commit where applicable, compatibility/removal versions, fixed type precedence, every intended event ID/index and complete semantic receipt payload, output byte length/SHA-256/event count/head, coverage classification, and findings. Event order is fixed by type precedence, stable pseudonymous source item ID, claim kind, locator, and digest. Each event ID is `mig:` plus lowercase SHA-256 of canonical JSON containing migration profile, all input digests, pseudonymous source identity or singleton semantic ID, event type, and zero-based ordinal. Native provider IDs never seed public identifiers. Genesis uses the bootstrap's exact frozen records and `authorization_request_commit`; legacy commitments are recomputed through the registered private provider.

The recovery manifest has exactly `schema_version:uint=1`, `plan_sha256:sha256`, `attestation_bundle_sha256:sha256`, `ledger_path:relative_path`, `ledger_sha256:sha256`, `transition_index:uint`, `phase`, `receipt_states[]`, `target_installed:boolean`, `anchor_receipt:receipt_ref|null`, `registry_before_sha256:sha256`, `registry_after_sha256:sha256|null`, `registry_successor_locator:locator|null`, `registry_successor_sha256:sha256|null`, `registry_successor_signature_locator:locator|null`, `registry_successor_signature_sha256:sha256|null`, `registry_cas_idempotency_key:sha256|null`, `registry_cas_request_locator:locator|null`, `registry_cas_request_sha256:sha256|null`, `registry_cas_request_signature_locator:locator|null`, `registry_cas_request_signature_sha256:sha256|null`, `registry_cas_result_locator:locator|null`, `registry_cas_result_sha256:sha256|null`, `registry_cas_result_signature_locator:locator|null`, `registry_cas_result_signature_sha256:sha256|null`, `recovery_preimage_locator:locator|null`, `recovery_preimage_sha256:sha256|null`, `recovery_request_locator:locator|null`, `recovery_request_sha256:sha256|null`, `recovery_request_signature_locator:locator|null`, `recovery_request_signature_sha256:sha256|null`, `recovery_blob_idempotency_key:sha256|null`, `recovery_blob_result_locator:locator|null`, `recovery_blob_result_sha256:sha256|null`, `recovery_blob_result_signature_locator:locator|null`, `recovery_blob_result_signature_sha256:sha256|null`, `recovery_blob_provider_commit_oid:git_oid|null`, `recovery_authorization_result_locator:locator|null`, `recovery_authorization_result_sha256:sha256|null`, `recovery_receipt:receipt_ref|null`, `recovery_publication_locator:locator|null`, and `recovery_publication_sha256:sha256|null`. Phase is `planned`, `receipts-published`, `target-installed`, `anchored`, `registry-prepared`, or `registry-committed` and never decreases. Each receipt state has exactly `event_id:id128`, `idempotency_key:sha256`, `payload_sha256:sha256`, `receipt_locator:locator`, `receipt_sha256:sha256`, `signature_locator:locator`, `signature_sha256:sha256`, `authorization_result_locator:locator`, `authorization_result_sha256:sha256`, `publication_result_locator:locator|null`, `publication_result_sha256:sha256|null`, and `state` (`authorized`, `refetched`, or `bound`). The state array exactly equals plan events and the attestation bundle in event-index order; omission, addition, reordering, or byte mismatch fails.

The initial manifest is uniquely `transition_index:0`, `phase:planned`, every receipt state `authorized` with exact bundle receipt/signature/authorization-result fields and null publication fields, `target_installed:false`, and every anchor/registry/CAS/recovery field null except the non-null before and bundle digests. Apply creates it durably before the first planned receipt append when both target and recovery manifest are absent and read-only lookup proves that no planned event or deterministic recovery receipt has been published. If the local manifest is missing but remote results exist, recovery scans every deterministic event/recovery idempotency key, requires planned events to form one event-index prefix, verifies each append/recovery result and preimage chain, reconstructs the unique highest manifest, and only then continues; a gap, fork, changed request, or result outside that chain fails closed. Apply verifies all event authority signatures, caller proofs, authenticated provider decisions, and fixed-provider policy bindings before writing transition-zero or publishing anything. After each event receipt is atomically appended and independently refetched, one state changes `authorized -> refetched`; only a prefix may be refetched in `planned`. If append succeeded before local update, recovery queries the receipt idempotency key and reconstructs the same result.

Each durable manifest transition increments `transition_index` by one and derives the exact manifest preimage with all seventeen current-transition recovery proof/publication fields null. It writes those bytes to deterministic `receipt_output/recovery-preimages/<receipt_state_sha256>.json`. From the plan digest, transition index, phase, and preimage digest, apply first derives the caller-independent blob-publication subject/idempotency key. From the plan's fixed recovery caller and deterministic content locator, apply constructs the complete `migration-recovery` receipt payload and authority request, invokes the registry-fixed `caller-sign-v1` process to obtain the recovery caller's request signature, then uses one caller-authenticated `publish-blob` CAS to publish exactly the manifest preimage, authority request, and caller signature. Only after generic lookup has recovered/refetched the signed blob result and all three immutable blobs does apply invoke the plan's fixed anchor-authority provider with their locators/digests. It preserves the returned receipt signature and authenticated decision, atomically appends/refetches the recovery receipt, and installs every recovery locator/digest in the local manifest. Apply may invoke only this plan-fixed recovery caller and recovery signer after transition zero; it never invokes an event caller or event-authority signer. A crash before blob CAS has no remote state; after blob CAS, the independently derivable publication key lets `lookup-provider-operation` recover the exact result/signature/introducing commit without the caller key, and later CAS losses are reconstructed from deterministic request/recovery-receipt/append keys without new attribution.

`receipts-published` requires all states `refetched`, `target_installed:false`, and no bound state. `target-installed` changes all states together `refetched -> bound`, requires `target_installed:true`, and bound is equivalent to installed. `anchored` additionally requires non-null verified `anchor_receipt`. Before CAS, `registry-prepared` durably stores non-null successor registry/signature locators/digests, CAS idempotency key, and request/request-signature locators/digests, while CAS result and after digest remain null; its recovery receipt and append result must be refetched before CAS. `registry-committed` requires refetched CAS result/signature and after digest equal the successor/authenticated result. Earlier phases require all later fields null/false and no phase skip. For transition `t`, the recovery semantic receipt has deterministic ID/time/idempotency from plan digest, transition index, phase, and manifest-preimage digest; publication obtains its independent append sequence. Remote-first recovery queries receipt and recovery idempotency keys; local state is never sole authority.

Planning writes no ledger, request, or receipt. Prepare writes only deterministic caller-signed event requests; attest runs once per event in its fixed-policy authority process; bundle proves exact plan/request/partial set equality. Apply rejects source/input/registry/plan/request/bundle/policy divergence, never invokes an event caller or event signer, and invokes only the separately frozen recovery pair for the dynamic transition protocol above. It idempotently appends/refetches each already-signed event receipt, follows the exact recovery prefix, then builds/validates/fsyncs/atomically installs the deterministic ledger. Provider timeout, plan mismatch, signature/policy failure, ID collision, partial write, or validation failure leaves target unchanged. Published attestations remain refetched but unbound `pending-orphan` receipts until identical retry installs the ledger and changes all states to bound. Replaying identical inputs produces byte-identical plan, requests, partials, bundle, ledger, semantic receipts/signatures, recovery requests/receipts, and existing provider results; new append ordering may interleave foreign valid receipts without changing semantic bytes. Migration remains `unanchored` until normal ledger publish/resolve succeeds.

Provider-native `registry-cas` normally runs only after anchor resolution. Its request payload has exactly `schema_version:uint=1`, `operation:registry-cas-literal`, `provider_id:id128`, `authorization_mode` (`normal` or `recovery`), `idempotency_key:sha256`, `expected_registry_sequence:uint`, `expected_registry_locator:locator`, `expected_registry_sha256:sha256`, `replacement_registry_locator:locator`, `replacement_registry_sha256:sha256`, `replacement_signature_locator:locator`, `replacement_signature_sha256:sha256`, `key_activation_prepares[]`, and `anchor_receipt:receipt_ref|null`. Under the provider mutation lock, the expected registry locator/sequence/digest must equal the initialized provider registry state or the latest accepted registry-CAS result; mismatch fails before object/control creation and consumes no sequence. Provider control head/sequence are selected from current protected state inside that same lock and appear only in the result/control, so publishing/refetching the pointer request on this provider cannot create a request-head fixed point. Each activation prepare ref has exactly `identity_kind`, `identity_id`, `provider_id`, `old_key_id`, `replacement_key_id`, `activation_request_locator:locator`, `activation_request_sha256:sha256`, `authority_bundle_locator:locator`, `authority_bundle_sha256:sha256`, `prepare_result_locator:locator`, `prepare_result_sha256:sha256`, `prepare_proof_locator:locator`, and `prepare_proof_sha256:sha256`, and the array is canonical-byte sorted and duplicate-free. The registered wire input is a pointer envelope with exactly `schema_version:uint=1`, `operation:registry-cas-literal`, `provider_id:id128`, `request_locator:locator`, `request_sha256:sha256`, `request_authorization_locator:locator`, and `request_authorization_sha256:sha256`; the invoked provider ID must equal both envelope and request, and it refetches all four immutable bytes/digests before evaluating the request. If the successor changes no key, the activation-prepare array is empty; otherwise it must bijectively equal the successor key delta and every request/bundle/result/proof tuple must refetch and verify before CAS. Each referenced prepare or rebind request's previous registry locator/digest must equal the CAS request's expected locator/digest, and its replacement locator/digest must equal the CAS request's replacement locator/digest, so aliases fail and one binding cannot authorize a request against another predecessor/successor. Recovery may reference a rebind of an ordinary pending generation only when the recovery bundle explicitly adopts that nonce and the rebind result/proof independently verifies the same identity/key/handle/generation; it never references the original continuity prepare for the recovery successor. Every other recovery tuple requires recovery-mode prepare authority. In normal mode the authorization blob is the predecessor registry-owner's detached signature over canonical request bytes in namespace `datarim-registry-cas-v1`, the anchor is non-null/verified, the predecessor key remains active through CAS even when registry-owner rotates, every ordinary key change resolves the old+new continuity bundle retained in its prepare tuple, and the replacement-signature blob is governance's signature over the successor registry. In recovery mode the replacement-signature locator resolves the previous-catalog quorum recovery bundle. The request-authorization locator instead resolves a recovery-CAS authorization having exactly `record` and `signatures[]`; its record has exactly `schema_version:uint=1`, `operation:registry-cas-literal`, `request_sha256:sha256`, `recovery_bundle_locator:locator`, `recovery_bundle_sha256:sha256`, and `issued_at:utc_seconds`, and the prior catalog quorum signs those exact bytes under `datarim-recovery-cas-authorization-v1`. Thus the quorum separately binds the complete executable CAS request after prepare proofs exist and the final successor digest, while the recovery bundle binds the predecessor digest, deterministic successor preimage, and every compromised/replacement key/governance/activation field; no compromised ordinary key is consulted.

Recovery may use a null anchor only when the current registry has `canon_epoch:null`, no ledger anchor has ever been accepted under that registry, and the successor preserves byte-identical `registry_id`, `project_id`, `repository_id`, `mode`, null `canon_epoch`, `verifiers`, and `source_commitment_policy`. For both anchored and null-anchor recovery, `sequence` increments exactly once, `previous_registry_sha256` equals the current digest, `framework_catalog_sha256` may change only to the quorum-authorized successor catalog, `allowed_signers_sha256` must be the exact view derived from the successor, `callers` and `signers` may alter only entries whose key IDs appear in the recovery record; every provider record—including its stable `result_signer_id`, executable, host authority, bootstrap tuple, and registration tuple—remains byte-identical. A signer or caller record whose key is compromised retains its stable signer/caller/provider identity and may change only key-dependent fields (`key_id`, fingerprint, public key, predecessor key, validity, and status) to the recovery-authorized replacement key; scopes, role, policy, provider/caller binding, and unrelated records remain byte-identical. `revocations` adds exactly the corresponding old-key removal records at the successor registry sequence. The replacement governance key is accepted only from the recovery record and then authenticates future normal changes; recovery cannot weaken provider rules, verifier floors, adoption mode, or epoch. Outside the null-anchor case, the existing non-null anchor and adoption state must be preserved or advanced by the normal transition rules.

The result payload has exactly `schema_version:uint=1`, `operation:registry-cas-literal`, `provider_id:id128`, `authorization_mode`, `idempotency_key:sha256`, `request_sha256:sha256`, `request_authorization_sha256:sha256`, `previous_provider_head:sha256`, `result_provider_head:sha256`, `provider_sequence:uint`, `previous_registry_sequence:uint`, `previous_registry_locator:locator`, `previous_registry_sha256:sha256`, `result_registry_sequence:uint`, `result_registry_sha256:sha256`, `result_locator:locator`, `result_signature_locator:locator`, and `result_signature_sha256:sha256`; the wire response has exactly `result`, `signature`, and `provider_commit_oid`. In normal mode the predecessor provider record's still-active fixed result-signer key signs under `datarim-registry-cas-result-v1`. Recovery uses the byte-identical provider record's same stable result signer ID: its pending quorum-authorized successor key signs only when that signer is in the recovery key delta and its activation prepare proves the key; otherwise its unchanged still-active predecessor-registry key signs. Neither branch permits a replacement provider or signer selection. `request_sha256` and `request_authorization_sha256` bind the complete executable authority wire. The result is valid only when `previous_registry_sequence == request.expected_registry_sequence`, `previous_registry_locator == request.expected_registry_locator`, `previous_registry_sha256 == request.expected_registry_sha256`, `result_registry_sequence == request.expected_registry_sequence + 1`, `result_registry_sha256 == request.replacement_registry_sha256`, `result_locator == request.replacement_registry_locator`, and the result signature locator/digest equal the request replacement-signature locator/digest; result locators identify the immutable successor registry and exact governance signature or recovery bundle. The operation applies the common one-CAS control/head/path/sequence contract above. Neither request nor result is a common receipt or contains its own digest. Identical retries and response-loss recovery use `lookup-provider-operation`; changed request or authorization bytes under one key fail. It transitions `legacy` to one-release dual-read and later irreversible strict, or `genesis` to verified non-null `canon_epoch`; recovery additionally permits immediate monotonic revocation/key replacement without weakening adoption state. After successful lookup/refetch, every activation finalize runs idempotently; until all required finalizations pass, the successor registry is authoritative but affected providers fail closed. A crash after anchor or registry publication resumes by generic lookup/refetch and finalize without remigration.

The registry-CAS idempotency key is SHA-256 of canonical JSON containing exactly operation, provider ID, authorization mode, expected registry sequence/locator/digest, replacement registry locator/digest, replacement signature locator/digest, the canonical ordered activation-prepare request/authority/result/proof locator/digest tuples, and anchor receipt locator/digest. The result registry/signature locators identify the immutable successor tuple, not the result object itself.

For recovery authentication, `receipt_state_sha256` hashes the canonical recovery manifest with exactly `recovery_preimage_locator`, `recovery_preimage_sha256`, `recovery_request_locator`, `recovery_request_sha256`, `recovery_request_signature_locator`, `recovery_request_signature_sha256`, `recovery_blob_idempotency_key`, `recovery_blob_result_locator`, `recovery_blob_result_sha256`, `recovery_blob_result_signature_locator`, `recovery_blob_result_signature_sha256`, `recovery_blob_provider_commit_oid`, `recovery_authorization_result_locator`, `recovery_authorization_result_sha256`, `recovery_receipt`, `recovery_publication_locator`, and `recovery_publication_sha256` replaced by null. The recovery receipt binds that preimage digest; its authority request binds the resolved preimage locator/digest. After blob publication, authorization, and atomic receipt append/refetch, the local view installs the exact seventeen proof/publication values. This removes self-reference while binding every other byte.

#### D-REQ-10: Security and evidence preservation

Verbatim external text is treated as data, never executed. CLI text inputs use files or JSON stdin, paths are root-confined, commitments are recomputed through the private provider, symlinks cannot escape the project root, and reports never print secrets or provider-native identifiers. Evidence paths are durable and project-relative; `/tmp` cannot satisfy retention.

#### D-REQ-11: Project registration and adapter boundary

The core validator remains project-agnostic. A governance-signed registration schema defines exact contract roots, authoritative source/child manifests, surface matrices, constrained adapter argv, disjoint signer roles/scopes, verifier revisions, revocation/rotation policy, and customer authority. Missing, mutable, unsafe, downgraded, unauthorized, revoked, non-executable, timed-out, oversized, or schema-invalid adapters/receipts fail only the phases that need them.

#### D-REQ-12: Honest genesis and independent reviews

TUNE-0585 follows eighteen ordered signed commits before general implementation—exact PRD/plan pair, bootstrap authorization, bootstrap RED, bootstrap source, bootstrap review, trust request, trust invocation authorization, trust provision, spec-review request, contract base, freeze, authorization marker, authorization request, attestation-input request, attestation-input invocation authorization, attestation plan, attestation requests, and authorization event. The sole bootstrap source is constrained by the directly signed zero-to-one contract and independent review; it is later revalidated by the canonical runner and never counts as canonical evidence by itself. Raw TDD remains development-only. Present-day signed mutation RED and restored GREEN, focused/full suites, subordinate exact-blob spec review before each authorized source boundary, and exact-head quality review after implementation are required. Canonical completion waits for merge and the selected protected release/registry anchors.

#### D-REQ-13: Privacy-safe monotonic authority and durable writes

Private verbatim bytes remain in an access-controlled source provider while public ledger records retain pseudonymous HMAC commitments. Every accepted ledger head is externally anchored to a retrievable immutable ledger blob. Canonical bytes are runtime-independent. Concurrent writers, retries, stale locks, crash points, disk-full, divergent heads, truncation, rollback, key rotation/revocation, and unauthorized/ambiguous supersession have deterministic fail-closed or recoverable behavior without losing acknowledged events.

## Validation Acceptance Criteria

### V-AC-1 — Atomic coverage

Provider-manifest omission/pagination, private-source deletion/alteration, public identifier leakage, HMAC key/domain/rotation mismatch, broken one-to-one, one-to-many, aggregate catch-all, missing/stale atomicity review, duplicate, dangling, and commitment fixtures fail; valid exhausted mappings with an exact-graph authenticated atomicity review pass.
Covers: D-REQ-01, D-REQ-02

### V-AC-2 — Pre-work gate

One mutation per capability class plus omitted/duplicate/foreign requirement binding, missing parent/evolution edge, mismatched branch requirement set, unresolvable contract-manifest locator/digest, `Unbound`, mutable ref, absent freeze/authorization marker, dirty or wrong base, and ancestry mutants fail; a fully pinned exact-denominator fixture passes.
Covers: D-REQ-03

### V-AC-3 — Review evolution

Source-kind relabel, missing requirement edge, request-branch requirement-set drift, unpinned, post-hoc, created/revised-without-prework-mutation, rejected-to-applied, rejected-to-freeze, reused-rejected-request-ID, unauthenticated-decline, and crash-resume-after-rejection fixtures fail independently; reused/revised/created/no-change controls with exact requirement edges, pinned artifacts, and a new-ID retry after rejection pass.
Covers: D-REQ-04

### V-AC-4 — Live close gate

Omitting each RED/GREEN, deploy, semantic-live, screenshot-cell, and disposition field fails independently; 404/generic-page, aliased-cell, forged-adapter, tool-only, returned, rejected, and deferred mutants remain RED.
Covers: D-REQ-06

### V-AC-5 — Derived epic coverage

Omitted provider source/child, uncovered remark, uncovered child, child cycle, cross-project child, enabling-only child, and narrated-complete mutants fail with exact IDs; a complete provider-backed acyclic graph passes.
Covers: D-REQ-07

### V-AC-6 — Command routing

Capability discovery proves all task-creating/advancing commands classify and gate armed work; L1, L2, quick, content, replay, and new-command mutants fail.
Covers: D-REQ-08

### V-AC-7 — Migration monotonicity

Exact `canon_epoch`, one-release dual read, strict-mode non-reversion, genuine-source import, and `legacy_claim_imported` non-coverage fixtures pass independently. Ambient-clock/unregistered-network access, byte/order drift, missing/wrong-role/wrong-policy event attestations, unauthorized caller or arbitrary-payload signing, append idempotency byte drift, changed source/registry between plan/attest/apply, existing-identical versus existing-different target, plan/bundle replay non-identity, invalid recovery phase/digest/signature, remote-published/local-missing recovery, temp/short-write/fsync failure, anchor-before-registry-CAS inversion, crash-after-anchor resume, and registry-CAS request/result stale-sequence/digest mutants each fail at their named boundary without changing an acknowledged target.
Covers: D-REQ-09

### V-AC-8 — Runner and adapter safety

Argv/no-shell controls, executable/registration trust, integer-minute timeout/output/env caps, an over-deadline process group with a spawned grandchild that is terminated and observed as normalized exit `143` with SIGTERM attribution, a SIGTERM-resistant control that records raw wait status and `forced_kill:true` without leaking a descendant, path confinement, commitment checks, forged/hash-only runner receipts, wrong-role/scope/audience/namespace, expired/revoked/downgraded profiles, forged customer dispositions, authenticated result validation, and evidence-receipt provenance mutations pass.
Covers: D-REQ-05, D-REQ-10, D-REQ-11

### V-AC-9 — Genesis and review

TUNE-0585's exact reviewed PRD/plan pair, bootstrap authorization/RED/source/review, trust request/invocation/provision, immutable spec-review request, contract base, genesis freeze, authorization marker, immutable authorization request, attestation-input request/invocation, attestation plan, attestation requests, and authorization event are eighteen ordered signed commits before general implementation. Bootstrap RED precedes bootstrap source and its exact-head review precedes use; raw TDD is not canonical evidence. Present-day signed mutation RED/restored GREEN, focused/full exact-head validation, English-only surface, exact-blob spec-review disposition, external non-recursive exact-head quality-review receipt, embedded Git-namespace tag signature, separate `datarim-release-v1` receipt, registry-successor CAS/refetch, and post-merge verification of current host rules plus full tag/commit/tree/ledger blob/event head/count are independently verified.
Covers: D-REQ-12

### V-AC-10 — Monotonic durability and privacy

Private-source redaction, cross-runtime canonical-byte vectors, missing immutable blob, anchored-head truncation/rollback/divergence, anchor self-reference absence, target/actor/conflict supersession, concurrent append, duplicate retry, stale lock, disk-full/short-write, process-death-before/after rename/publication, directory-fsync, anchor-timeout-after-accept, and unanchored recovery fixtures each preserve acknowledged bytes/history and fail closed until reconciled.
Covers: D-REQ-13

## Error Handling

The validator uses stable exit codes: `0` phase satisfied, `1` contract/coverage finding, `2` usage or ledger-integrity failure, `3` adapter unavailable for a required phase. Findings include task ID, requirement ID, field/code, and remediation phase. Unknown schema versions, event types, enum values, broken hash chains, and escaped evidence paths fail closed.

## Rollout

1. Ship and release the universal framework mechanism through a protected PR.
2. Register TALO-0001 in an isolated Arcanada/Talomnia adoption task.
3. Import all customer remarks and canonical White Paper figures without inventing evidence.
4. Derive exact open requirement IDs.
5. Execute reversible Talomnia work requirement-by-requirement.
6. Deploy exact merged revisions, capture the live matrix, and record customer dispositions.

## Risks

- **False completeness through adapters:** constrained authenticated adapters provide data only; the core recomputes coverage and state against authoritative manifests and pinned trust roots.
- **Post-hoc timestamps:** same-repo ancestry and retained binding records are authoritative; timestamps alone never prove ordering.
- **Local rollback:** hash chaining alone is insufficient, so no stage transition trusts an unanchored local head.
- **Privacy leak:** public ledgers retain only versioned HMAC commitments and opaque locators by default; verbatim bytes and HMAC keys remain in the registered private source provider.
- **Artifact explosion:** one task-level JSONL ledger and retained evidence directories keep the model bounded.
- **Legacy noise:** the pivot is default-strict for new tasks and opt-in strict for pre-pivot tasks.
- **Customer authority ambiguity:** registration names accepted authority kinds; agents cannot author `accepted` dispositions unless explicitly authorized.

## Downstream Boundary

This PRD is complete when the universal mechanism is released. It does not claim that Talomnia is delivered. TALO-0001 remains open until its imported requirement graph, sixteen canonical White Paper figures, product implementation, exact deployment, live screenshot matrix, and customer dispositions pass the new derived coverage gate.
