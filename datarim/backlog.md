# Backlog

## Pending
- TUNE-0532 · pending · P2 · L2 · Add domain-specific security guidance for new tech-stack domains (CLI, Desktop, Systems, Data/ML, WASM) → tasks/TUNE-0532-task-description.md
- TUNE-0533 · pending · P3 · L2 · Monitor toolchain reform effectiveness — if "Recommended Toolchains" proves rigid in practice, spawn lightweight toolchain-choice mechanism → tasks/TUNE-0533-task-description.md
- TUNE-0582 · pending · P3 · L1 · `dev-tools/check-deploy-class.sh:63` arms the prod-readiness gate on EVERY NestJS/Angular task, because the `\.service\b` indicator sits in the same alternation as `systemd`/`systemctl` and matches `*.service.ts` FILENAMES rather than systemd units. Reproduced live on the Mac runtime 2026-08-16: a task description naming only `prisma-bi.service.ts` and `field-registry.service.ts` exits 0 (deploy-class, gate MUST arm) with zero deploy surface in the diff; controls confirm the classifier is otherwise sane (plain prose -> exit 1, a real `systemctl`/`/etc/sudoers` task -> exit 0). In Nest/Angular repos `.service` is the dominant file-naming convention, so the gate arms essentially always and its signal collapses toward zero — the cry-wolf failure where a reviewer learns to wave it through. The existing negation filter does not help: it only suppresses lines carrying an explicit negation phrase. FIX: require unit-file context for the systemd sense (`\.service` preceded by a path separator, or adjacent to `systemctl`/`systemd`/`/etc/`), or drop bare `\.service\b`. Add a regression test to `tests/dr-deploy-class-detector.bats` (currently zero occurrences of `service.ts`) asserting a `*.service.ts`-only description is NOT deploy-class, plus a positive control so the fix cannot be satisfied by disarming the gate. Discovered during `/dr-qa` of DEV-1747 in the Aether workspace 2026-08-11 by another agent; deliberately kept out of the Aether epic because it is a framework defect. Evidence preserved in `documentation/archive/quick/archive-QCK-0076.md` § Third defect (source artefacts were on the operator's Desktop and are disposable once this row exists).
- TUNE-0584 · done · P2 · L2 · Fix the exit-code conflation left open by TUNE-0583: `eh_decision` returned 0 for BOTH on-host AND unconfigured, so a guard that was never installed was indistinguishable from one that ran and passed — both are silence, and a machine with no protection read as healthy. Minting a new exit code was verified WRONG against the real consumer: its `case` dispatches unknown codes into the deny arm, so every unconfigured workspace worldwide would have started denying. Instead the resolver now sets EH_STATE (on-host|unconfigured|off-host|fail-closed|readonly-bypass) in the CALLER's shell — exit codes byte-for-byte unchanged, all 35 pre-existing tests pass untouched. Not exported (a stale exported copy would look authoritative); read-only intent is labelled readonly-bypass, not on-host, because it short-circuits before any host resolution. ADDED dev-tools/check-execution-host-health.sh: proves a machine is gated via a POSITIVE control (current host resolves on-host) AND a NEGATIVE control (foreign hostname must be denied) — a control never observed denying has not been observed at all; reports UNCONFIGURED out loud instead of passing quietly, fails closed on env errors. Tests +9 EH_STATE (44 total) + 10 health incl. TWO MUTATION CONTROLS that seed a non-denying resolver and assert the check goes red. Verified live on all three fleet hosts with both controls; surfaced a real fact previously hidden as silence — a privileged shell whose HOME cannot see the user-local map answers UNCONFIGURED while the owning user answers on-host. Full suite 3350 tests, 0 failures. → documentation/archive/framework/archive-TUNE-0584.md
- TUNE-0583 · done · P2 · L2 · Stop shipping the execution-host PreToolUse guard: the framework now ships MECHANISM, not enforcement POLICY. Root cause was NOT 'the shipped copy went stale' but two writable copies of one ENFORCEMENT artefact in two repos with no sync — so updating the shipped copy would have re-armed the identical failure at the next edit. The drifted copy was wrong in BOTH directions at once: on one consumer host it denied every task ON ITS OWN declared required_host (the resolver there answered correctly; the stale guard never consulted it), while a second host had no protection at all. A four-agent consilium (security / SRE / architect / strategist) converged unanimously on removal, drawing the line at mechanism-vs-policy: a resolver answering 'which host is declared' is generic; a hook DECIDING that work elsewhere is forbidden encodes one site's topology and, for a single-machine user, can only ever deny. REMOVED: dev-tools/datarim-exec-guard.sh + its 28-test bats (moved to the site workspace, where all 28 still pass). KEPT: lib/execution-host.sh, check-execution-host-drift.sh, tests/execution-host.bats, session-execution-drift-warn.sh. ADDED: dev-tools/check-class-b-not-shipped.sh — a ship-time gate so the rule stops being prose only (TUNE-0500 declared this class-b and was silently violated by TUNE-0519); explicit denylist, not a *-guard.sh heuristic that would misfire on the generic branch-integration-guard. DOCS: 7 shipped surfaces said 'the framework ships the PreToolUse guard — install it as a hook'; all rewritten to a capability statement. Two pointed at dev-tools/datarim-dispatch.sh, ALREADY absent from this repo — a documented path to a nonexistent file, i.e. a stranger's first contact with the framework was a broken instruction. fleet-hook-sync.md told operators to symlink the guard OUT OF THE FRAMEWORK RUNTIME — the precise instruction that caused the original defect; now corrected to link from the site's own repo. S10-bis rewritten and now warns about the trap that hid the outage: eh_decision returns 0 for BOTH on-host AND unconfigured, so a fail-open no-op is indistinguishable from a healthy pass — assert a deny under a foreign hostname, never infer health from silence. Side benefit: 13 lines of Russian left the public surface with the guard (English-only rule). No topology leaked either way — IPs/hosts live only in the gitignored map and space.yml. VERIFIED: gate red→green on the real change plus a seeded positive control; exec-guard-wiring.bats rewritten (14/14, assertions inverted on purpose); all three fleet hosts pass positive AND negative controls and none now resolves its guard through a framework clone; simulated a framework update with the guard deleted — protection survived. → documentation/archive/framework/archive-TUNE-0583.md
- TUNE-0596 · pending · P2 · L2 · [Found 2026-08-25 while dispatching QCK-0079 from the control machine; ID probed free via next-free-id.sh] The Step-0 EXECUTION HOST block tells every `/dr-*` command to `source "${DATARIM_RUNTIME:-$HOME/.claude}/dev-tools/lib/execution-host.sh"`, but the library is bash (`#!/usr/bin/env bash`, `# shellcheck shell=bash`) and uses bash-only syntax — `read -a` in `eh_host_match`. Sourcing it from a zsh agent shell (the default on macOS, and the operator's control machine) aborts the function with `eh_host_match:read:15: bad option: -a`, and the caller observes exit 10. MEASURED, both directions: same workspace, same map — `source` under zsh → `bad option: -a`, exit 10 with EH_STATE unset; `bash -c 'source ...; eh_decision ...'` → exit 10 with `EH_STATE=off-host`. The verdict happened to be correct here (the Mac genuinely is `role: control`), which is exactly what makes this dangerous: THE GATE RETURNED THE RIGHT ANSWER FOR THE WRONG REASON, and a crash is indistinguishable from a real off-host verdict at the call site. The failure is not confined to one command — the identical `source` line appears in the Step-0 block of every `/dr-*` command (dr-archive, dr-auto, dr-compliance, dr-design, dr-next, dr-quick, dr-do, …), so on any non-bash agent shell the whole Step-0 contour degrades to "always exit 10". Note the blast radius runs BOTH ways: 10 is fail-closed for a mutating command (annoying but safe), but the crash also destroys the on-host and unconfigured verdicts, so a genuine execution host would be told it is off-host and told to delegate to itself. This is the same defect class TUNE-0584 closed for exit-code conflation — a caller cannot distinguish two states — except the conflation here is between "resolver spoke" and "resolver died". DoD: (1) make the contract explicit and mechanical — either invoke the library through `bash` regardless of the agent's shell (change the Step-0 instruction in every command that carries it) or make the library POSIX/zsh-safe, and say which in the header; do not leave it to the caller's shell to decide; (2) POSITIVE CONTROL IS MANDATORY and is the whole point of this row — assert that a crash inside the resolver is NOT reported as a clean verdict: a deliberately broken/unsourceable library must produce a distinguishable failure (exit 3, the existing fail-closed code) and never exit 10, verified by a test that goes red without the fix; (3) run the bats suite under zsh as well as bash, or the regression returns silently on the next bash-ism. Archive subdir: framework/.
