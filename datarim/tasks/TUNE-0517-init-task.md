---
task_id: TUNE-0517
artifact: init-task
schema_version: 1
captured_at: 2026-07-26
captured_by: /dr-init
operator: operator
status: canonical
source: /dr-init
---

# TUNE-0517 - Init-Task (canonical operator brief)

## Source command

```
/dr-init TUNE-0517
```

## Operator brief (verbatim)

You are a dedicated Datarim framework orchestrator for TUNE-0517. Read /tmp/TUNE-0517-brief.txt in full, then execute the complete task autonomously in the PUBLIC Datarim framework repo at ~/arcanada/Projects/Datarim/code/datarim/. First add the TUNE-0517 one-liner to datarim/backlog.md (Pending section) from the brief header, then run the full lifecycle /dr-init -> /dr-plan -> /dr-do -> /dr-qa -> /dr-compliance -> /dr-archive. Own worktree + own signed branch. Commit AND push each status flip immediately (dogfood). SHIPPED-SURFACE HYGIENE IS CRITICAL: English-only, zero operator-specific names (no Arcanada/DEVS/Aether/tailscale/space.yml/hostnames/IPs), Class A advisory-only, no routing or exit-code changes, version-awareness must be fail-open. When de-pinning model IDs, verify current vendor defaults against real vendor docs (use context7 MCP) - do NOT invent model IDs; if uncertain prefer vendor-default SEMANTICS over a guessed ID. grep-verify no non-ASCII and no operator literals before committing. Autonomous; hard-gate only on prod deploy / public release / secret rotation (none expected). Start now.

The complete canonical task specification is `/tmp/TUNE-0517-brief.txt`, read
in full at task start. Its requirements are reproduced in the sibling task
description and expectations checklist without narrowing.

## Append-log (operator amendments)

_(empty at creation)_
