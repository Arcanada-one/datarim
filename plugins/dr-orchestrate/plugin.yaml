schema_version: 1
id: dr-orchestrate
title: "Datarim Orchestrator (Phase 2 — Subagent Inference + Bot-Interaction Interface)"
version: 0.4.0
author: "Pavel Valentov / Arcanada"
license: MIT
description: "tmux-based self-driving Datarim pipeline runner — Phase 2 adds multi-backend subagent inference for unknown prompts. Autonomy is resolved per-space from space.yml § autonomy.policy via resolve-space-autonomy.sh / action_gate.sh — full-autonomy spaces run all reversible actions without asking; only the Supreme-Directive hard-gated floor escalates to the operator. v0.3.0 adds bot-interaction interface: OpenAPI 3.1 inbound contract + adnanh/webhook reference impl + HMAC-SHA256 outbound emitter + Redis pub/sub opt-in backend."
categories:
  - scripts
  - rules
# NOTE (TUNE-0439): the /dr-orchestrate command + resolver agent were promoted to
# core (commands/, agents/) so the Claude Code slash-picker discovers them
# (it does not scan plugins/*/commands/ or ~/.claude/local/). This plugin now
# ships the orchestration scripts, rules, openapi, and tests only.
homepage: https://datarim.club/commands/dr-orchestrate
# Rollout gate (TUNE-0187): this plugin hosts autonomous agents, so `/dr-plugin
# enable` refuses activation until the consumer ecosystem CLAUDE.md mirrors the
# canonical Autonomous Agent Operating Rules (FB-1..FB-8) at rank-1 mandate
# level. See documentation/reference/fb-rules-consumer-rollout.md.
requires_fb_rules_mirror: true
